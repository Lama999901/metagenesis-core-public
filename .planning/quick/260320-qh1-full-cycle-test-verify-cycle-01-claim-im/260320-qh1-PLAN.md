---
phase: quick
plan: 260320-qh1
type: execute
wave: 1
depends_on: []
files_modified: []  # Net zero — all changes reverted
autonomous: true
must_haves:
  truths:
    - "agent_impact.py detects new_claim rule when VERIFY-CYCLE-01 claim files are committed"
    - "All required files per new_claim rule show as updated (0 MISSING, excluding sealed canonical_state.md)"
    - "Repository returns to exact pre-test state after revert"
  artifacts: []  # Net zero plan — no persistent artifacts
  key_links:
    - from: "backend/progress/verify_cycle_01.py"
      to: "agent_impact.py new_claim trigger"
      via: "git diff detection of new .py in backend/progress/"
      pattern: "backend/progress/.*\\.py"
---

<objective>
Full-cycle test of the NEW CLAIM protocol: create a trivial claim VERIFY-CYCLE-01 following
all 6 mandatory steps from CLAUDE.md, commit it, run agent_impact.py to verify 0 MISSING
files, then hard-reset to undo everything. Net code changes = ZERO.

Purpose: Prove agent_impact.py correctly detects all dependency files for a new_claim change.
Output: Console output from agent_impact.py showing PASS (captured in SUMMARY.md).
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@scripts/agent_impact.py

Key detection logic from agent_impact.py:
- new_claim triggers when a NEW .py file appears in backend/progress/ (not runner.py)
- Required files for new_claim rule:
  1. backend/progress/runner.py
  2. reports/scientific_claim_index.md
  3. system_manifest.json
  4. index.html
  5. README.md
  6. AGENTS.md
  7. llms.txt
  8. CONTEXT_SNAPSHOT.md
- Also triggers new_tests rule (test file in tests/), adding same files minus runner.py and claim_index
- canonical_state.md is NOT in agent_impact.py's required_files list — so no exemption needed

Current counters (from system_manifest.json):
- active_claims: 15 (array of 15 IDs)
- test_count: 544
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all VERIFY-CYCLE-01 claim artifacts and commit</name>
  <files>
    backend/progress/verify_cycle_01.py
    backend/progress/runner.py
    reports/scientific_claim_index.md
    tests/verify_cycle/test_verify_cycle_01.py
    system_manifest.json
    index.html
    README.md
    AGENTS.md
    llms.txt
    CONTEXT_SNAPSHOT.md
  </files>
  <action>
IMPORTANT: Record the current commit hash FIRST with `git rev-parse HEAD` — store it for Task 3 revert.

1. Create branch: `git checkout -b test/verify-cycle` (local only, never push)

2. Create `backend/progress/verify_cycle_01.py`:
   - Trivial claim that always passes (e.g., checks 1+1==2)
   - Must have full 4-step Step Chain using the exact template from CLAUDE.md
   - JOB_KIND = "verify_cycle_01"
   - Returns standard dict: mtr_phase, inputs, result, execution_trace, trace_root_hash

3. Edit `backend/progress/runner.py`:
   - Add a dispatch block for job_kind "verify_cycle_01" that imports and calls the claim
   - Place it after the last existing dispatch block (search for the last elif/if pattern)
   - Keep it minimal: just import and call

4. Append to `reports/scientific_claim_index.md`:
   - Add VERIFY-CYCLE-01 entry at the bottom following the existing table format
   - claim_id: VERIFY-CYCLE-01, domain: Testing, job_kind: verify_cycle_01

5. Create `tests/verify_cycle/test_verify_cycle_01.py`:
   - Must have __init__.py in tests/verify_cycle/ directory
   - Basic test: import the claim function, run it, assert pass==True
   - Assert execution_trace has 4 steps
   - Assert trace_root_hash is a 64-char hex string

6. Update counters — in ALL these files, increment claims count 15->16 and test count 544->547 (3 new tests):
   - `system_manifest.json`: Add "VERIFY-CYCLE-01" to active_claims array, update test_count to 547
   - `README.md`: Find claim count references (15) and bump to 16, test count 544->547
   - `AGENTS.md`: Same counter updates
   - `llms.txt`: Same counter updates
   - `CONTEXT_SNAPSHOT.md`: Same counter updates
   - `index.html`: All 11+ places with claim/test counts — use search-and-replace carefully

7. Stage ALL modified/new files and commit:
   ```
   git add backend/progress/verify_cycle_01.py backend/progress/runner.py \
     reports/scientific_claim_index.md tests/verify_cycle/ \
     system_manifest.json index.html README.md AGENTS.md llms.txt CONTEXT_SNAPSHOT.md
   git commit -m "test: VERIFY-CYCLE-01 full claim cycle (will revert)"
   ```

DO NOT push. This branch is local-only.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "VERIFY-CYCLE-01" && echo "PASS: commit exists" || echo "FAIL"</automated>
  </verify>
  <done>All 10 files created/modified in a single commit on test/verify-cycle branch. The commit contains a new claim file, runner dispatch, claim index entry, test file, and counter bumps in 6 doc/config files.</done>
</task>

<task type="auto">
  <name>Task 2: Run agent_impact.py and verify 0 MISSING</name>
  <files></files>
  <action>
Run agent_impact.py in both modes and capture output:

1. Full output mode:
   ```
   python scripts/agent_impact.py --verify-last-commit
   ```
   Expected output: "All N required files updated. PASS" with 0 MISSING lines.

2. Summary mode:
   ```
   python scripts/agent_impact.py --summary
   ```
   Expected: "new_claim,new_tests | N/N files updated | PASS"

3. Capture both outputs for SUMMARY.md evidence.

4. If any files show as MISSING:
   - Read the output to identify which files
   - Go back and update those files in an additional commit
   - Re-run agent_impact.py to confirm 0 MISSING

The key success signal is the word "PASS" in the output and 0 lines starting with "  ! " (the MISSING indicator).
  </action>
  <verify>
    <automated>python scripts/agent_impact.py --summary 2>&amp;1 | grep -q "PASS" && echo "PASS: 0 missing" || echo "FAIL: files missing"</automated>
  </verify>
  <done>agent_impact.py reports PASS with 0 MISSING files for the VERIFY-CYCLE-01 commit. Both full and summary output captured as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Revert all changes and verify clean state</name>
  <files></files>
  <action>
Revert everything to restore the repo to its pre-test state:

1. Return to the original branch:
   ```
   git checkout feat/agent-divine-v2
   ```

2. Delete the test branch:
   ```
   git branch -D test/verify-cycle
   ```

3. Remove any leftover untracked files from the test:
   ```
   # Check if tests/verify_cycle/ directory still exists
   # If so, remove it manually
   ```

4. Verify clean state:
   ```
   git diff HEAD --stat
   git status
   ```
   Should show no unexpected changes (only the same untracked files that existed before this task).

5. Verify key files are back to original state:
   - `system_manifest.json` should have test_count: 544 and 15 claims
   - `backend/progress/verify_cycle_01.py` should NOT exist
   - `tests/verify_cycle/` directory should NOT exist

The repo MUST be identical to its state before Task 1 began.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('system_manifest.json')); assert d['test_count']==544; assert len(d['active_claims'])==15; print('PASS: counters restored')" && test ! -f backend/progress/verify_cycle_01.py && echo "PASS: claim file gone" || echo "FAIL"</automated>
  </verify>
  <done>Repository is back to exact pre-test state. No claim file, no test directory, all counters at original values. Net code changes = ZERO.</done>
</task>

</tasks>

<verification>
1. agent_impact.py --summary output contains "PASS" (captured in Task 2)
2. system_manifest.json has exactly 15 claims and test_count 544 (verified in Task 3)
3. backend/progress/verify_cycle_01.py does NOT exist (verified in Task 3)
4. Current branch is feat/agent-divine-v2 (verified in Task 3)
</verification>

<success_criteria>
- agent_impact.py detected "new_claim" change type for the VERIFY-CYCLE-01 commit
- agent_impact.py reported 0 MISSING files (PASS)
- All changes fully reverted — net code changes = ZERO
- Evidence captured in SUMMARY.md for future reference
</success_criteria>

<output>
After completion, create `.planning/quick/260320-qh1-full-cycle-test-verify-cycle-01-claim-im/260320-qh1-SUMMARY.md`
</output>
