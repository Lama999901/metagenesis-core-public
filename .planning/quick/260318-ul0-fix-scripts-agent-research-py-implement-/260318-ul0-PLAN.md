---
phase: quick
plan: 260318-ul0
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/agent_research.py
  - AGENT_TASKS.md
autonomous: true
must_haves:
  truths:
    - "execute_task_002() reads backend claim files and returns a real design spec for AGENT-DRIFT-01"
    - "execute_task_003() runs check_stale_docs and audits index.html, returns real findings"
    - "execute_task_004() reads paper.md and returns 5 real JOSS reviewer questions with answers"
    - "execute_task_005() reads relevant backend files and returns a real MLflow/DVC/WandB integration API sketch"
    - "Re-running TASK-004 as a new PENDING task produces a report with substantive content, not 'Not yet implemented'"
  artifacts:
    - path: "scripts/agent_research.py"
      provides: "Real execution logic for all 5 task handlers"
  key_links:
    - from: "scripts/agent_research.py"
      to: "paper.md, index.html, backend/progress/*.py, scripts/check_stale_docs.py"
      via: "file reads and subprocess calls in each handler"
      pattern: "read_text|subprocess.run"
---

<objective>
Implement real execution logic for task handlers 002-005 in scripts/agent_research.py.
Currently these return "Not yet implemented" stubs. Each handler must read actual
repo files and produce substantive analysis content. Then re-test by running TASK-004
as a new pending task to verify real output.

Purpose: The agent research system is useless with stub handlers -- reports contain no content.
Output: Working agent_research.py with all 5 handlers producing real analysis.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/agent_research.py
@AGENT_TASKS.md
@paper.md
@index.html
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement all four stub handlers in agent_research.py</name>
  <files>scripts/agent_research.py</files>
  <action>
Replace the four stub functions (execute_task_002 through execute_task_005) with real implementations.
Each handler must read actual repo files and produce markdown analysis. Use the existing
execute_task_001 as the quality reference -- it reads files, builds tables, produces structured markdown.

**execute_task_002 -- Design claim 15 AGENT-DRIFT-01:**
- Read CLAUDE.md section "AGENT-DRIFT-01" for the baseline spec
- Read backend/progress/runner.py to understand dispatch pattern
- Read an existing claim file (e.g., backend/progress/drift_monitor.py) as template
- Produce a design spec with: job_kind, threshold, step chain structure (4 steps),
  input/output schema, physical anchor decision (none -- meta-claim),
  required test categories, estimated implementation scope
- Return structured markdown with code blocks showing proposed schema

**execute_task_003 -- Audit index.html and stale docs:**
- Run `python scripts/check_stale_docs.py` via subprocess and capture output
- Read index.html and search for key counters: test count (511), claim count (14),
  layer count (5), innovation count (8), version strings (v0.5.0)
- For each counter, find all occurrences with line numbers using enumerate over lines
- List any mismatches or stale references found
- Return markdown with: check_stale_docs output, counter audit table (counter | expected | line | actual), findings

**execute_task_004 -- Predict JOSS reviewer questions:**
- Read paper.md fully (REPO_ROOT / "paper.md")
- Read paper.bib if it exists
- Analyze the paper structure: title, abstract, statement of need, key claims
- Based on standard JOSS review criteria (functionality, documentation, tests, state of the field,
  statement of need, installation, example usage, community guidelines), generate 5 likely questions
- For each question: the question text, which JOSS criterion it targets,
  the prepared answer referencing specific repo files/sections, and the confidence level
- Return structured markdown with numbered Q&A pairs

**execute_task_005 -- Draft integration API sketch:**
- Read scripts/mg.py to understand the verify/pack CLI interface
- Read backend/progress/runner.py to understand job dispatch
- Draft a Python API sketch showing how MLflow, DVC, and WandB could integrate:
  - MLflow: post-training hook that calls mg.py pack + verify
  - DVC: pipeline stage that produces verification bundle
  - WandB: callback that logs verification status as artifact
- Include actual code examples (not pseudocode) using the real mg.py CLI flags
- Return structured markdown with code blocks for each integration

All handlers must:
- Use REPO_ROOT / path to read files (same pattern as execute_task_001)
- Return a multi-line markdown string (not "Not yet implemented")
- Handle missing files gracefully with try/except
- Produce at least 30 lines of substantive content
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0,'.'); from scripts.agent_research import execute_task_002, execute_task_003, execute_task_004, execute_task_005; results = [execute_task_002(), execute_task_003(), execute_task_004(), execute_task_005()]; assert all(len(r) > 200 and 'Not yet implemented' not in r for r in results), f'Stub or too short: {[len(r) for r in results]}'; print('ALL HANDLERS OK')"</automated>
  </verify>
  <done>All four handlers return substantive markdown content (200+ chars each, no "Not yet implemented")</done>
</task>

<task type="auto">
  <name>Task 2: Re-test by running TASK-004 as new pending task</name>
  <files>AGENT_TASKS.md</files>
  <action>
1. Read AGENT_TASKS.md
2. Reset TASK-004 status from "DONE (2026-03-18)" back to "PENDING"
   (so agent_research.py picks it up as the first pending task)
3. Run: python scripts/agent_research.py
4. Verify the generated report in reports/AGENT_REPORT_*.md contains real JOSS reviewer
   questions (not "Not yet implemented")
5. Check report file has at least 5 numbered questions with answers
6. After verification succeeds, the task stays DONE (agent_research.py marks it done again)

If the report contains "Not yet implemented" or is too short, the task has failed.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; from datetime import datetime; p = Path('reports') / f'AGENT_REPORT_{datetime.now().strftime(\"%Y%m%d\")}.md'; content = p.read_text(encoding='utf-8'); assert 'TASK-004' in content or 'JOSS' in content or 'reviewer' in content.lower(), 'No JOSS content'; assert len(content) > 500, f'Too short: {len(content)}'; assert 'Not yet implemented' not in content, 'Still stub'; print(f'Report OK: {len(content)} chars')"</automated>
  </verify>
  <done>AGENT_REPORT file for today contains real JOSS reviewer questions (500+ chars, no stubs). TASK-004 marked DONE again. Branch fix/agent-research-impl created and pushed.</done>
</task>

</tasks>

<verification>
1. All 5 handlers in agent_research.py return real content
2. No handler returns "Not yet implemented"
3. Re-running TASK-004 produces a substantive report
4. All existing tests still pass: python -m pytest tests/ -q
</verification>

<success_criteria>
- scripts/agent_research.py has real implementation for execute_task_002 through execute_task_005
- Each handler reads actual repo files and produces structured markdown analysis
- Running agent_research.py with TASK-004 pending produces a report with 5 real JOSS reviewer questions
- Changes committed to fix/agent-research-impl branch and pushed
</success_criteria>

<output>
After completion, create `.planning/quick/260318-ul0-fix-scripts-agent-research-py-implement-/260318-ul0-SUMMARY.md`
</output>
