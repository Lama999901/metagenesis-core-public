---
phase: quick
plan: 260320-k8t
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check_stale_docs.py
autonomous: true
must_haves:
  truths:
    - "tests/test_coverage_boost.py is tracked in CONTENT_CHECKS with required=['generate_key', 'run_self_test'] and banned=[]"
    - "auto_watchlist_scan.py reports 0 unwatched files"
    - "check_stale_docs.py --strict exits 0"
  artifacts:
    - path: "scripts/check_stale_docs.py"
      provides: "CONTENT_CHECKS entry for tests/test_coverage_boost.py"
      contains: "tests/test_coverage_boost.py"
  key_links: []
---

<objective>
Add tests/test_coverage_boost.py to the CONTENT_CHECKS watchlist in scripts/check_stale_docs.py so the auto-watchlist scanner no longer flags it as unwatched.

Purpose: Close watchlist gap -- test_coverage_boost.py was added in quick task 260320-icp but not registered in CONTENT_CHECKS.
Output: Updated check_stale_docs.py with new entry, both scanners passing.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/check_stale_docs.py
@tests/test_coverage_boost.py (first ~10 lines for imports: generate_key from mg_sign, run_self_test from mg_ed25519)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_coverage_boost.py to CONTENT_CHECKS and verify both scanners pass</name>
  <files>scripts/check_stale_docs.py</files>
  <action>
In scripts/check_stale_docs.py, add a new entry to the CONTENT_CHECKS dict (after the last existing entry, before the closing brace around line 246):

```python
    "tests/test_coverage_boost.py": {
        "banned": [],
        "required": ["generate_key", "run_self_test"],
    },
```

The required strings confirm the file still imports and tests the two key functions from mg_sign.py and mg_ed25519.py that the coverage boost targets.

Then verify:
1. `python scripts/auto_watchlist_scan.py` -- confirm 0 unwatched files
2. `python scripts/check_stale_docs.py --strict` -- confirm exit 0
3. Checkout branch fix/watchlist-544, commit, and push.
  </action>
  <verify>
    <automated>python scripts/auto_watchlist_scan.py --strict && python scripts/check_stale_docs.py --strict</automated>
  </verify>
  <done>
- tests/test_coverage_boost.py appears in CONTENT_CHECKS with required=["generate_key", "run_self_test"] and banned=[]
- auto_watchlist_scan.py --strict exits 0 (0 unwatched)
- check_stale_docs.py --strict exits 0
- Changes committed to fix/watchlist-544 and pushed
  </done>
</task>

</tasks>

<verification>
python scripts/auto_watchlist_scan.py --strict && python scripts/check_stale_docs.py --strict
Both must exit 0.
</verification>

<success_criteria>
- CONTENT_CHECKS contains "tests/test_coverage_boost.py" entry
- Zero unwatched files reported by auto_watchlist_scan.py
- check_stale_docs.py --strict passes (exit 0)
- Committed and pushed to fix/watchlist-544
</success_criteria>

<output>
After completion, create `.planning/quick/260320-k8t-add-test-coverage-boost-py-to-content-ch/260320-k8t-SUMMARY.md`
</output>
