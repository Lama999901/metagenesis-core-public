---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system_manifest.json
  - README.md
  - CLAUDE.md
  - CITATION.cff
  - llms.txt
  - CONTEXT_SNAPSHOT.md
  - CURSOR_MASTER_PROMPT_v2_3.md
  - AGENTS.md
  - index.html
  - docs/AGENT_SYSTEM.md
  - docs/HOW_TO_ADD_CLAIM.md
  - docs/REAL_DATA_GUIDE.md
  - docs/USE_CASES.md
  - reports/scientific_claim_index.md
  - scripts/check_stale_docs.py
autonomous: true
requirements: [v0.7.0-release]

must_haves:
  truths:
    - "Every file referencing v0.6.0 or v0.6 now references v0.7.0 or v0.7"
    - "check_stale_docs.py required strings updated from v0.6 to v0.7"
    - "All verification gates pass (steward_audit, pytest, deep_verify, check_stale_docs)"
    - "Changes committed to feat/v070-release branch and pushed"
  artifacts:
    - path: "system_manifest.json"
      provides: "version 0.7.0 and github_release v0.7.0"
      contains: "0.7.0"
    - path: "README.md"
      provides: "badge and version line with v0.7.0"
      contains: "v0.7.0"
  key_links:
    - from: "scripts/check_stale_docs.py"
      to: "CITATION.cff, llms.txt, CONTEXT_SNAPSHOT.md, CURSOR_MASTER_PROMPT_v2_3.md"
      via: "required string checks"
      pattern: "v0\\.7"
---

<objective>
Update all version references from v0.6.0 to v0.7.0 across the entire codebase, verify all gates pass, commit and push to feat/v070-release branch.

Purpose: Prepare the v0.7.0 release by bumping version strings in all files. GitHub Release creation happens AFTER merge to main.
Output: All version references updated, gates passing, branch pushed.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@system_manifest.json
@CLAUDE.md
@README.md (lines 1-15)
@CITATION.cff
@scripts/check_stale_docs.py (lines 70-150 for required strings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all version references 0.6.0 to 0.7.0</name>
  <files>
    system_manifest.json,
    README.md,
    CLAUDE.md,
    CITATION.cff,
    llms.txt,
    CONTEXT_SNAPSHOT.md,
    CURSOR_MASTER_PROMPT_v2_3.md,
    AGENTS.md,
    index.html,
    docs/AGENT_SYSTEM.md,
    docs/HOW_TO_ADD_CLAIM.md,
    docs/REAL_DATA_GUIDE.md,
    docs/USE_CASES.md,
    reports/scientific_claim_index.md,
    scripts/check_stale_docs.py
  </files>
  <action>
    Create branch feat/v070-release from current HEAD (feat/agent-divine-v2).

    Update version references in each file. Use grep to find all occurrences first, then edit precisely.

    **system_manifest.json:**
    - "version": "0.6.0" -> "0.7.0"
    - "protocol": "MetaGenesis Verification Protocol (MVP) v0.6" -> "v0.7"
    - "github_release": "v0.6.0" -> "v0.7.0"

    **README.md:**
    - Badge: Protocol-MVP%20v0.6.0 -> Protocol-MVP%20v0.7.0
    - Version line: v0.6.0 -> v0.7.0

    **CLAUDE.md:**
    - All "v0.6.0" -> "v0.7.0" (Release line, header references, WHAT'S NEXT section)
    - "v0.6" standalone references -> "v0.7" where appropriate (MVP v0.6 -> MVP v0.7)
    - Footer version line
    - NOTE: Keep "v0.6.0" in historical entries (e.g., "v0.6.0 IDEAS" section header, completed milestones). Only update references to CURRENT version.

    **CITATION.cff:**
    - version: 0.6.0 -> 0.7.0

    **llms.txt:**
    - Line 6: "MVP) v0.6" -> "MVP) v0.7"

    **CONTEXT_SNAPSHOT.md:**
    - "GitHub Release | v0.6.0" -> "v0.7.0"

    **CURSOR_MASTER_PROMPT_v2_3.md:**
    - Line 11: "v0.6.0" -> "v0.7.0"
    - Line 29: "MVP v0.6" -> "MVP v0.7"

    **AGENTS.md:**
    - Line 30: "MVP) v0.6" -> "MVP) v0.7"

    **index.html:**
    - Line 1581: v0.6.0 -> v0.7.0 (version badge in hero)
    - Line 3007: "MVP v0.6" -> "MVP v0.7" (footer)

    **docs/AGENT_SYSTEM.md:**
    - Line 96 footer: v0.6.0 -> v0.7.0

    **docs/HOW_TO_ADD_CLAIM.md:**
    - Line 219 footer: v0.6 -> v0.7

    **docs/REAL_DATA_GUIDE.md:**
    - Line 200 footer: v0.6 -> v0.7

    **docs/USE_CASES.md:**
    - Line 253 footer: v0.6 -> v0.7

    **reports/scientific_claim_index.md:**
    - Line 369 footer: v0.6 -> v0.7

    **scripts/check_stale_docs.py:**
    - Line 76: "version: 0.6" -> "version: 0.7" (CITATION.cff required string)
    - Line 132: "MVP v0.6" -> "MVP v0.7"
    - Line 136: "v0.6" -> "v0.7"
    - Line 140: "v0.6" -> "v0.7"
    - Line 144: "v0.6" -> "v0.7"
    - Line 248: "v0.6.0" -> "v0.7.0"

    Do NOT touch docs/ROADMAP.md line 93 ("Digital Twin Verification Path (v0.6+)") -- that is a historical roadmap reference, not a current version string.

    Do NOT touch reports/ chronicle or signal files -- those are historical records of v0.6.0 work.

    Do NOT touch .claude/worktrees/ -- those are ephemeral worktree copies.

    After all edits, run: `grep -rn "0\.6\.0\|v0\.6[^+]" --include="*.md" --include="*.json" --include="*.txt" --include="*.html" --include="*.cff" --include="*.py" . | grep -v "\.git/" | grep -v "\.claude/worktrees" | grep -v "reports/CHRONICLE" | grep -v "reports/SIGNALS" | grep -v "reports/AGENT_REPORT" | grep -v "reports/COVERAGE" | grep -v "reports/SELF_IMPROVEMENT" | grep -v "node_modules"` to verify no stale references remain in active files.
  </action>
  <verify>
    <automated>cd /c/Users/999ye/Downloads/metagenesis-core-public && grep -rn "v0\.6\.0" system_manifest.json README.md CLAUDE.md CITATION.cff llms.txt CONTEXT_SNAPSHOT.md CURSOR_MASTER_PROMPT_v2_3.md AGENTS.md index.html docs/AGENT_SYSTEM.md scripts/check_stale_docs.py | head -5; echo "---"; grep "0\.7\.0" system_manifest.json | head -3</automated>
  </verify>
  <done>Zero v0.6.0 references remain in active files (excluding historical reports and .claude/worktrees). All active files show v0.7.0 or v0.7.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification gates, commit, and push</name>
  <files>None (verification and git operations only)</files>
  <action>
    Run all four verification gates in order:

    1. `python scripts/steward_audit.py` -- must output "STEWARD AUDIT: PASS"
    2. `python -m pytest tests/ -q` -- must show all tests passing (544 expected)
    3. `python scripts/deep_verify.py` -- must output "ALL 13 TESTS PASSED"
    4. `python scripts/check_stale_docs.py` -- must pass (no stale docs)

    If any gate fails, diagnose and fix the issue (likely a missed version reference in check_stale_docs required strings).

    Once all gates pass:
    - `git checkout -b feat/v070-release` (if not already created in Task 1)
    - `git add` all modified files by name (list each file explicitly -- never use `git add .`)
    - `git commit -m "release: bump version 0.6.0 -> 0.7.0 across all files"`
    - `git push origin feat/v070-release`

    Do NOT create a GitHub Release. That happens after merge to main.
  </action>
  <verify>
    <automated>cd /c/Users/999ye/Downloads/metagenesis-core-public && python scripts/steward_audit.py && python -m pytest tests/ -q && python scripts/deep_verify.py && python scripts/check_stale_docs.py</automated>
  </verify>
  <done>All 4 gates pass. Changes committed to feat/v070-release and pushed to origin. No GitHub Release created yet.</done>
</task>

</tasks>

<verification>
1. `grep -rn "v0\.7\.0" system_manifest.json README.md CITATION.cff` shows v0.7.0 in all three
2. All four verification gates pass
3. `git log --oneline -1` shows version bump commit on feat/v070-release
4. `git branch -vv` confirms feat/v070-release tracks origin
</verification>

<success_criteria>
- Every active file references v0.7.0 (not v0.6.0)
- check_stale_docs.py required strings updated to expect v0.7
- All 4 verification gates pass (steward_audit, pytest 544, deep_verify 13, check_stale_docs)
- Commit on feat/v070-release branch, pushed to origin
- No GitHub Release created (deferred to post-merge)
</success_criteria>

<output>
After completion, create `.planning/quick/260321-d9w-v0-7-0-release-update-manifest-readme-ba/260321-d9w-SUMMARY.md`
</output>
