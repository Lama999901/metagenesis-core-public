---
phase: quick
plan: 260320-jt4
type: execute
wave: 1
depends_on: []
files_modified:
  - system_manifest.json
  - index.html
  - CLAUDE.md
  - AGENTS.md
  - CITATION.cff
  - CONTEXT_SNAPSHOT.md
  - CONTRIBUTING.md
  - CURSOR_MASTER_PROMPT_v2_3.md
  - README.md
  - llms.txt
  - paper.md
  - ppa/README_PPA.md
  - docs/ARCHITECTURE.md
  - docs/HOW_TO_ADD_CLAIM.md
  - docs/REAL_DATA_GUIDE.md
  - docs/ROADMAP.md
  - docs/USE_CASES.md
  - reports/known_faults.yaml
  - reports/scientific_claim_index.md
  - scripts/check_stale_docs.py
autonomous: true
requirements: [COUNTER-SYNC]
must_haves:
  truths:
    - "All documentation files show 544 tests instead of 532"
    - "check_stale_docs.py CONTENT_CHECKS reference 544 not 532"
    - "pytest confirms 544 tests pass"
    - "steward_audit PASS"
    - "check_stale_docs --strict PASS"
  artifacts:
    - path: "system_manifest.json"
      provides: "test_count: 544"
      contains: "544"
    - path: "index.html"
      provides: "12 places updated from 532 to 544"
    - path: "scripts/check_stale_docs.py"
      provides: "CONTENT_CHECKS entries reference 544"
  key_links:
    - from: "scripts/check_stale_docs.py"
      to: "all doc files"
      via: "CONTENT_CHECKS required strings"
      pattern: "544"
---

<objective>
Replace test counter 532 with 544 across all documentation, index.html, system_manifest.json, and the stale-docs checker itself.

Purpose: After adding 12 new tests (coverage boost), all public-facing and internal counters must reflect the actual test count of 544.
Output: All files updated, all verification gates pass.
</objective>

<context>
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace 532 with 544 in all documentation and index.html</name>
  <files>
    system_manifest.json, index.html, CLAUDE.md, AGENTS.md, CITATION.cff,
    CONTEXT_SNAPSHOT.md, CONTRIBUTING.md, CURSOR_MASTER_PROMPT_v2_3.md,
    README.md, llms.txt, paper.md, ppa/README_PPA.md, docs/ARCHITECTURE.md,
    docs/HOW_TO_ADD_CLAIM.md, docs/REAL_DATA_GUIDE.md, docs/ROADMAP.md,
    docs/USE_CASES.md, reports/known_faults.yaml, reports/scientific_claim_index.md
  </files>
  <action>
For each file listed, do a global find-and-replace of "532" with "544" for test-counter occurrences. Be precise -- only replace occurrences that refer to the test count, NOT arbitrary "532" substrings in hashes or UUIDs.

Specific replacements by file (all confirmed as test-counter references):

1. **system_manifest.json** line 18: `"test_count": 532` -> `"test_count": 544`

2. **index.html** (12 occurrences, all test counters):
   Use PowerShell: `(Get-Content index.html -Raw) -replace '532', '544' | Set-Content index.html -NoNewline`
   This is safe because all 12 instances of "532" in index.html are test counters (confirmed by grep).

3. **CLAUDE.md** (5 occurrences): lines 4, 44, 53, 85, 244
4. **AGENTS.md** (1): line 107
5. **CITATION.cff** (1): line 21
6. **CONTEXT_SNAPSHOT.md** (3): lines 25, 96, 109
7. **CONTRIBUTING.md** (3): lines 12, 17, 145
8. **CURSOR_MASTER_PROMPT_v2_3.md** (7): lines 11, 29, 79, 170, 186, 194, 376
9. **README.md** (7): lines 8, 186, 278, 323, 510, 536, 583
10. **llms.txt** (2): lines 103, 109
11. **paper.md** (1): line 57
12. **ppa/README_PPA.md** (1): line 42
13. **docs/ARCHITECTURE.md** (1): line 260
14. **docs/HOW_TO_ADD_CLAIM.md** (1): line 219
15. **docs/REAL_DATA_GUIDE.md** (1): line 200
16. **docs/ROADMAP.md** (2): lines 37, 100
17. **docs/USE_CASES.md** (1): line 253
18. **reports/known_faults.yaml** (4): lines 4, 11, 17, 47
19. **reports/scientific_claim_index.md** (1): line 369

For text files, use sed or PowerShell global replace. For each file, verify with `grep -c "532" FILE` that zero occurrences remain, and `grep -c "544" FILE` shows the expected count.

IMPORTANT: Do NOT modify sealed files (steward_audit.py, mg_policy_gate_policy.json, etc.).
IMPORTANT: Do NOT modify files under demos/ or reports/progress_runs/ -- those contain "532" in hash strings, not counters.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && grep -rn "532" system_manifest.json CLAUDE.md AGENTS.md CITATION.cff CONTEXT_SNAPSHOT.md CONTRIBUTING.md CURSOR_MASTER_PROMPT_v2_3.md README.md llms.txt paper.md ppa/README_PPA.md docs/ARCHITECTURE.md docs/HOW_TO_ADD_CLAIM.md docs/REAL_DATA_GUIDE.md docs/ROADMAP.md docs/USE_CASES.md reports/known_faults.yaml reports/scientific_claim_index.md index.html | grep -v "progress_runs" | grep -v "demos/" ; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero occurrences of "532" remain in any of the 19 listed files. All show "544" in the correct places.</done>
</task>

<task type="auto">
  <name>Task 2: Update check_stale_docs.py CONTENT_CHECKS entries from 532 to 544</name>
  <files>scripts/check_stale_docs.py</files>
  <action>
In scripts/check_stale_docs.py, the CONTENT_CHECKS dict has "required" arrays that reference "532" as a required string for various files. Replace all occurrences of "532" with "544" in these required/banned string lists.

Known lines with "532" (from grep):
- Line 72: required: ["532", ...]
- Line 76: required: [..., "532"]
- Line 84: required: ["532 tests", ...]
- Line 88: required: [..., "532 adversarial"]
- Line 92: required: ["532 tests", ...]
- Line 108: required: ["532", ...]
- Line 112: required: ["532 adversarial", ...]
- Line 116: required: ["532 tests", "532 passed"]
- Line 128: required: ["532 tests", ...]
- Line 132: required: ["532", ...]
- Line 136: required: ["532", ...]
- Line 140: required: ["532", ...]
- Line 144: required: [..., "532"]
- Line 184: required: ["532", ...]
- Line 192: required: ["532", ...]

Use global find-replace of "532" with "544" in this file. All occurrences are test counter references in CONTENT_CHECKS required strings.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && grep -c "532" scripts/check_stale_docs.py && echo "FAIL: still has 532" || echo "PASS: no 532 left"</automated>
  </verify>
  <done>scripts/check_stale_docs.py has zero "532" references. All CONTENT_CHECKS entries now require "544".</done>
</task>

<task type="auto">
  <name>Task 3: Run verification gates and commit</name>
  <files></files>
  <action>
1. Run pytest to confirm 544 tests pass:
   `python -m pytest tests/ -q`
   Expected: "544 passed"

2. Run steward_audit:
   `python scripts/steward_audit.py`
   Expected: "STEWARD AUDIT: PASS"

3. Run check_stale_docs --strict:
   `python scripts/check_stale_docs.py --strict`
   Expected: all checks pass (no stale docs)

4. If all gates pass, create branch and commit:
   ```
   git checkout -b fix/counter-544
   git add system_manifest.json index.html CLAUDE.md AGENTS.md CITATION.cff CONTEXT_SNAPSHOT.md CONTRIBUTING.md CURSOR_MASTER_PROMPT_v2_3.md README.md llms.txt paper.md ppa/README_PPA.md docs/ARCHITECTURE.md docs/HOW_TO_ADD_CLAIM.md docs/REAL_DATA_GUIDE.md docs/ROADMAP.md docs/USE_CASES.md reports/known_faults.yaml reports/scientific_claim_index.md scripts/check_stale_docs.py
   git commit -m "fix: sync test counter 532 -> 544 across all docs"
   git push origin fix/counter-544
   ```

5. If any gate fails, fix the issue before committing.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python -m pytest tests/ -q 2>&1 | tail -3 && python scripts/steward_audit.py 2>&1 | tail -1 && python scripts/check_stale_docs.py --strict 2>&1 | tail -3</automated>
  </verify>
  <done>All 3 verification gates pass (544 tests, steward audit PASS, stale docs clean). Changes committed to fix/counter-544 and pushed.</done>
</task>

</tasks>

<verification>
1. `grep -rn "532" system_manifest.json index.html CLAUDE.md AGENTS.md CITATION.cff CONTEXT_SNAPSHOT.md CONTRIBUTING.md CURSOR_MASTER_PROMPT_v2_3.md README.md llms.txt paper.md ppa/README_PPA.md docs/*.md reports/known_faults.yaml reports/scientific_claim_index.md scripts/check_stale_docs.py` returns NOTHING
2. `python -m pytest tests/ -q` shows "544 passed"
3. `python scripts/steward_audit.py` shows "STEWARD AUDIT: PASS"
4. `python scripts/check_stale_docs.py --strict` passes clean
</verification>

<success_criteria>
- All 19 doc files + check_stale_docs.py updated from 532 to 544
- All 3 verification gates pass
- Changes on fix/counter-544 branch, committed and pushed
</success_criteria>

<output>
After completion, create `.planning/quick/260320-jt4-counter-sync-532-544-across-all-docs-and/260320-jt4-SUMMARY.md`
</output>
