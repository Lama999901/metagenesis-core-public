---
phase: quick
plan: 260318-s3k
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENT_TASKS.md
  - scripts/agent_research.py
  - .planning/config.json
  - reports/AGENT_REPORT_20260318.md
autonomous: true
must_haves:
  truths:
    - "AGENT_TASKS.md exists with 5 tasks in English, each with Status/Priority/Output"
    - "scripts/agent_research.py reads AGENT_TASKS.md, executes first PENDING task, writes report"
    - "TASK-001 executed and report written to reports/AGENT_REPORT_20260318.md"
  artifacts:
    - path: "AGENT_TASKS.md"
      provides: "Task registry for agent research system"
    - path: "scripts/agent_research.py"
      provides: "Agent research runner script"
    - path: "reports/AGENT_REPORT_20260318.md"
      provides: "Research findings from TASK-001"
  key_links:
    - from: "scripts/agent_research.py"
      to: "AGENT_TASKS.md"
      via: "file read/parse/update"
    - from: "scripts/agent_research.py"
      to: "scripts/agent_learn.py"
      via: "subprocess call (observe)"
---

<objective>
Create an agent research system: a task registry (AGENT_TASKS.md) and a Python runner (agent_research.py) that picks the first PENDING task, executes it by analyzing actual repo source code, writes findings to a dated report, and marks the task DONE. Then run it for TASK-001 (test coverage audit).

Purpose: Automate recurring research/audit tasks so agents can self-improve the protocol.
Output: AGENT_TASKS.md, scripts/agent_research.py, reports/AGENT_REPORT_20260318.md
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@scripts/agent_evolution.py (UTF-8 fix pattern, subprocess pattern, REPO_ROOT pattern)
@scripts/agent_learn.py (called via subprocess at end of research run)
@.planning/config.json (add agent_research.py to post_milestone_tasks)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AGENT_TASKS.md and scripts/agent_research.py</name>
  <files>AGENT_TASKS.md, scripts/agent_research.py, .planning/config.json</files>
  <action>
**1. Create AGENT_TASKS.md** in repo root with exactly this structure (all English):

```markdown
# Agent Research Tasks

Auto-processed by `scripts/agent_research.py`. First PENDING task gets executed each run.

## Tasks

### TASK-001
- **Title:** Audit test coverage per all 14 claims
- **Status:** PENDING
- **Priority:** P1
- **Output:** reports/AGENT_REPORT_YYYYMMDD.md
- **Description:** For each of the 14 active claims (MTR-1, MTR-2, MTR-3, SYSID-01, DATA-PIPE-01, DRIFT-01, ML_BENCH-01, DT-FEM-01, ML_BENCH-02, ML_BENCH-03, PHARMA-01, FINRISK-01, DT-SENSOR-01, DT-CALIB-LOOP-01), count test files and test functions. Find the claim with weakest coverage. Propose 3 new adversarial tests.

### TASK-002
- **Title:** Design claim 15 AGENT-DRIFT-01
- **Status:** PENDING
- **Priority:** P1
- **Output:** reports/AGENT_REPORT_YYYYMMDD.md
- **Description:** Design AGENT-DRIFT-01 with job_kind, threshold, step chain structure, physical anchor decision, and estimate implementation hours.

### TASK-003
- **Title:** Audit index.html and stale docs for v0.5.0
- **Status:** PENDING
- **Priority:** P2
- **Output:** reports/AGENT_REPORT_YYYYMMDD.md
- **Description:** Run check_stale_docs --strict and audit index.html for v0.5.0/JOSS/Ed25519/NLnet/Agent Evolution references. List exact line fixes needed.

### TASK-004
- **Title:** Predict JOSS reviewer questions
- **Status:** PENDING
- **Priority:** P2
- **Output:** reports/AGENT_REPORT_YYYYMMDD.md
- **Description:** Read paper.md, identify 5 likely JOSS reviewer questions with prepared answers.

### TASK-005
- **Title:** Draft integration API sketch
- **Status:** PENDING
- **Priority:** P3
- **Output:** docs/INTEGRATION_GUIDE.md
- **Description:** Draft MLflow/DVC/WandB integration API sketch for docs/INTEGRATION_GUIDE.md.
```

**2. Create scripts/agent_research.py** with these specifications:

- Copy the UTF-8 fix pattern from agent_evolution.py (lines 22-25: sys.stdout/stderr wrapping).
- Copy the REPO_ROOT pattern from agent_evolution.py (line 27).
- Copy the subprocess `run()` helper from agent_evolution.py (lines 42-47) with PYTHONIOENCODING=utf-8.

Core logic:

```python
def parse_tasks(content):
    """Parse AGENT_TASKS.md, return list of dicts with id, title, status, priority, output, description."""
    # Split on ### TASK- headers, regex parse each block for Status/Priority/Output/Description

def find_first_pending(tasks):
    """Return first task with status PENDING, or None."""

def execute_task(task):
    """Dispatch to task-specific handler based on task id."""
    handlers = {
        "TASK-001": execute_task_001,
        "TASK-002": execute_task_002,
        "TASK-003": execute_task_003,
        "TASK-004": execute_task_004,
        "TASK-005": execute_task_005,
    }
    handler = handlers.get(task["id"])
    if not handler:
        return f"No handler for {task['id']}"
    return handler()

def execute_task_001():
    """Audit test coverage for all 14 claims."""
    # 1. Define the 14 claims with their file paths from CLAUDE.md
    claims = {
        "MTR-1": "backend/progress/mtr1_calibration.py",
        "MTR-2": "backend/progress/mtr2_thermal_conductivity.py",
        # ... all 14
    }

    # 2. For each claim, search tests/ recursively for test files mentioning the claim ID or its filename stem
    #    Count: number of test files, number of test functions (def test_*)
    #    Use Path.rglob and text search, NOT subprocess grep

    # 3. Build a coverage table: claim_id | impl_file | test_files | test_count

    # 4. Find weakest claim (fewest tests)

    # 5. Propose 3 adversarial tests for the weakest claim:
    #    - Test that modifies a step chain hash and expects FAIL
    #    - Test that strips a required field and expects semantic catch
    #    - Test that replays an old bundle with new timestamp

    # Return markdown string with full report

# Stub handlers for TASK-002 through TASK-005 (just return "Not yet implemented" string)
# These will be filled in when those tasks are executed

def mark_task_done(task_id, tasks_path):
    """Read AGENT_TASKS.md, replace 'Status: PENDING' with 'Status: DONE (YYYY-MM-DD)' for the given task."""
    content = tasks_path.read_text(encoding="utf-8")
    # Find the task section and replace its status line
    # Use regex: within the TASK-{id} section, replace PENDING with DONE (date)
    tasks_path.write_text(updated_content, encoding="utf-8")

def write_report(task, findings):
    """Write findings to reports/AGENT_REPORT_YYYYMMDD.md"""
    report_path = REPO_ROOT / "reports" / f"AGENT_REPORT_{datetime.now().strftime('%Y%m%d')}.md"
    # Header: # Agent Research Report - TASK-{id}: {title}
    # Date, task description
    # Then the findings markdown
    report_path.write_text(report_content, encoding="utf-8")
    return report_path

def main():
    tasks_path = REPO_ROOT / "AGENT_TASKS.md"
    if not tasks_path.exists():
        print("ERROR: AGENT_TASKS.md not found"); sys.exit(1)

    content = tasks_path.read_text(encoding="utf-8")
    tasks = parse_tasks(content)
    task = find_first_pending(tasks)
    if not task:
        print("No PENDING tasks found"); sys.exit(0)

    print(f"Executing {task['id']}: {task['title']}")
    findings = execute_task(task)
    report_path = write_report(task, findings)
    mark_task_done(task["id"], tasks_path)
    print(f"Report written to {report_path}")

    # Run agent_learn.py observe
    import os
    env = os.environ.copy()
    env["PYTHONIOENCODING"] = "utf-8"
    subprocess.run(
        [sys.executable, str(REPO_ROOT / "scripts" / "agent_learn.py"), "observe"],
        cwd=REPO_ROOT, env=env, encoding="utf-8", errors="replace"
    )
    print("Done.")
```

IMPORTANT for execute_task_001:
- MUST actually read source files using Path operations (not stubs)
- For each of the 14 claims, search all test files in tests/ recursively
- Use `path.read_text(encoding="utf-8", errors="ignore")` for reading
- Search for claim ID patterns: lowercase, underscore variants (e.g., "mtr1", "mtr_1", "MTR-1", "mtr1_calibration")
- Count `def test_` occurrences in matching files
- Sort claims by test count ascending to find weakest
- The 3 proposed adversarial tests should reference the weakest claim's specific implementation details

**3. Update .planning/config.json:**
Add `"python scripts/agent_research.py"` to the `post_milestone_tasks` array (append after existing 3 entries).
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; assert Path('AGENT_TASKS.md').exists(); assert Path('scripts/agent_research.py').exists(); print('Files exist')"</automated>
  </verify>
  <done>AGENT_TASKS.md has 5 tasks with PENDING status. agent_research.py has working parse/execute/report/mark logic. config.json updated.</done>
</task>

<task type="auto">
  <name>Task 2: Run agent_research.py for TASK-001 and verify output</name>
  <files>reports/AGENT_REPORT_20260318.md, AGENT_TASKS.md</files>
  <action>
Run the script:
```bash
cd C:/Users/999ye/Downloads/metagenesis-core-public
python scripts/agent_research.py
```

Verify:
1. reports/AGENT_REPORT_20260318.md exists and contains a coverage table for all 14 claims
2. AGENT_TASKS.md shows TASK-001 as DONE with today's date
3. The report identifies the weakest claim and proposes 3 adversarial tests

If the script fails, debug and fix. Common issues:
- Encoding errors on Windows: ensure UTF-8 wrappers are in place
- Path issues: ensure REPO_ROOT is resolved correctly
- Missing test directories: some claims may have tests in unexpected locations (cross-domain), handle gracefully
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; r=Path('reports/AGENT_REPORT_20260318.md'); assert r.exists() and len(r.read_text(encoding='utf-8'))>500, 'Report too short or missing'; t=Path('AGENT_TASKS.md').read_text(encoding='utf-8'); assert 'DONE' in t, 'TASK-001 not marked DONE'; print('PASS')"</automated>
  </verify>
  <done>TASK-001 executed. Report in reports/AGENT_REPORT_20260318.md contains per-claim coverage table, weakest claim identified, 3 adversarial tests proposed. AGENT_TASKS.md shows TASK-001 as DONE.</done>
</task>

<task type="auto">
  <name>Task 3: Branch, commit, and push</name>
  <files></files>
  <action>
```bash
cd C:/Users/999ye/Downloads/metagenesis-core-public
git checkout -b feat/agent-research
git add AGENT_TASKS.md scripts/agent_research.py reports/AGENT_REPORT_*.md .planning/config.json
git commit -m "feat: add agent research system with TASK-001 coverage audit

- AGENT_TASKS.md: 5 research tasks for agent self-improvement
- scripts/agent_research.py: auto-executes PENDING tasks, writes reports
- reports/AGENT_REPORT_20260318.md: TASK-001 coverage audit results
- .planning/config.json: added agent_research to post_milestone_tasks"
git push origin feat/agent-research
```

Note: Do NOT run verification gates (steward_audit, pytest, deep_verify) -- these new files are additive documentation/tooling, not modifying any sealed or tested code.
  </action>
  <verify>
    <automated>git log --oneline -1 | grep "agent research"</automated>
  </verify>
  <done>All files committed on feat/agent-research branch and pushed to origin.</done>
</task>

</tasks>

<verification>
1. AGENT_TASKS.md exists in repo root with 5 tasks, TASK-001 marked DONE
2. scripts/agent_research.py is executable and follows project UTF-8/encoding patterns
3. reports/AGENT_REPORT_20260318.md has substantive coverage analysis (not stubs)
4. .planning/config.json includes agent_research.py in post_milestone_tasks
5. All committed on feat/agent-research branch
</verification>

<success_criteria>
- AGENT_TASKS.md has 5 well-defined tasks with Status/Priority/Output fields
- agent_research.py successfully executed TASK-001 by reading actual test files
- Report contains per-claim coverage table with test counts for all 14 claims
- Weakest claim identified with 3 concrete adversarial test proposals
- Changes committed and pushed on feat/agent-research
</success_criteria>

<output>
After completion, create `.planning/quick/260318-s3k-agent-research-system-agent-tasks-md-age/260318-s3k-SUMMARY.md`
</output>
