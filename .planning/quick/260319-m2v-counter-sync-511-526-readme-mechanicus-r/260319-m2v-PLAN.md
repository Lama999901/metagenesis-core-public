---
phase: quick
plan: 260319-m2v
type: execute
wave: 1
depends_on: []
files_modified:
  - system_manifest.json
  - README.md
  - CLAUDE.md
  - AGENTS.md
  - llms.txt
  - index.html
  - CONTEXT_SNAPSHOT.md
  - ppa/README_PPA.md
  - paper.md
  - reports/known_faults.yaml
  - CONTRIBUTING.md
  - UPDATE_PROTOCOL.md
autonomous: true
requirements: [COUNTER-SYNC, README-REWRITE]
must_haves:
  truths:
    - "Zero occurrences of '511 tests' or '511 passed' remain in any target file"
    - "All target files show 526 as the test count"
    - "README.md has Mechanicus theme with mission statement, parallel table, founder story, and proof section"
    - "All existing README technical content preserved (badges, quick start, architecture, claims table)"
  artifacts:
    - path: "system_manifest.json"
      provides: "test_count: 526"
      contains: "526"
    - path: "README.md"
      provides: "Mechanicus-themed comprehensive README"
      contains: "Omnissiah"
    - path: "index.html"
      provides: "12 occurrences updated from 511 to 526"
  key_links:
    - from: "system_manifest.json"
      to: "all other files"
      via: "test_count value consistency"
      pattern: "526"
---

<objective>
Sync all test counters from 511 to 526 across every documentation and config file, then rewrite README.md with a Mechanicus theme while preserving all technical content.

Purpose: Test count is stale at 511 (actual is 526 after CERT gap tests). README needs personality and the full founder story.
Output: All counters synced, README rewritten, committed to fix/readme-mechanicus branch and pushed.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@system_manifest.json
@README.md
@CLAUDE.md
@index.html

BANNED TERMS (from CLAUDE.md):
- NEVER write "tamper-proof" -- use "tamper-evident"
- NEVER write "blockchain" -- use "cryptographic hash chain"
- NEVER write "unforgeable"
- NEVER write "100% test success" -- use "526 tests PASS"

SEALED FILES -- DO NOT MODIFY:
- scripts/steward_audit.py
- scripts/mg_policy_gate_policy.json
- ppa/CLAIMS_DRAFT.md
- reports/canonical_state.md
- demos/open_data_demo_01/evidence_index.json
- .github/workflows/total_audit_guard.yml
- .github/workflows/mg_policy_gate.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branch and sync all test counters 511 to 526</name>
  <files>
    system_manifest.json
    README.md
    CLAUDE.md
    AGENTS.md
    llms.txt
    index.html
    CONTEXT_SNAPSHOT.md
    ppa/README_PPA.md
    paper.md
    reports/known_faults.yaml
    CONTRIBUTING.md
    UPDATE_PROTOCOL.md
  </files>
  <action>
1. Create and checkout branch: `git checkout -b fix/readme-mechanicus`

2. For each file, replace ALL occurrences of the test count from 511 to 526. The specific replacements:

   **system_manifest.json:** Change `"test_count": 511` to `"test_count": 526`

   **index.html (12 occurrences -- use PowerShell per CLAUDE.md BUG 7):**
   Run: `powershell -Command "(Get-Content index.html -Raw) -replace '511', '526' | Set-Content index.html -NoNewline"`
   This covers all 12 occurrences including:
   - "MIT . 511 tests" in nav
   - Counter display spans (multiple)
   - "511 passed" in terminal output
   - "511 tests including adversarial proof"
   - JavaScript counter animation target `ct(document.getElementById('cn2'),511,1500)`
   IMPORTANT: After replacement, verify the JS counter target changed to 526: `ct(document.getElementById('cn2'),526,1500)`

   **CLAUDE.md:** Replace `511 tests` with `526 tests`, `511 passed` with `526 passed`, and the header `v0.5.0 LIVE | 14 claims | 511 tests` similarly.

   **README.md:** Replace all `511` with `526` (badge, pytest output, AI agent section). The badge line: `[![Tests](https://img.shields.io/badge/Tests-526%20passing-brightgreen.svg)](tests/)`

   **AGENTS.md:** Replace `511` with `526` everywhere.

   **llms.txt:** Replace `511` with `526` everywhere.

   **CONTEXT_SNAPSHOT.md:** Replace `511` with `526` everywhere.

   **ppa/README_PPA.md:** Replace `511` with `526` everywhere.

   **paper.md:** Replace `511` with `526` everywhere.

   **reports/known_faults.yaml:** Replace `511` with `526` everywhere.

   **CONTRIBUTING.md:** Replace `511` with `526` everywhere.

   **UPDATE_PROTOCOL.md:** Replace `511` with `526` everywhere.

3. After all replacements, run a grep to verify ZERO remaining 511 occurrences in target files:
   `grep -rn "511" system_manifest.json README.md CLAUDE.md AGENTS.md llms.txt index.html CONTEXT_SNAPSHOT.md ppa/README_PPA.md paper.md reports/known_faults.yaml CONTRIBUTING.md UPDATE_PROTOCOL.md`
   Expected: no output (zero matches). If any remain, fix them.

4. Also check ancillary files that showed 511 hits:
   - reports/scientific_claim_index.md
   - docs/USE_CASES.md, docs/REAL_DATA_GUIDE.md, docs/ROADMAP.md, docs/ARCHITECTURE.md, docs/HOW_TO_ADD_CLAIM.md
   - CURSOR_MASTER_PROMPT_v2_3.md, EVOLUTION_LOG.md
   Update these too if they contain test counter references (not just random number 511 in other contexts).

5. Do NOT modify sealed files (reports/canonical_state.md, ppa/CLAIMS_DRAFT.md, scripts/steward_audit.py, etc.)

6. Commit: `git add -A && git commit -m "fix: sync test counters from 511 to 526 across all docs"`
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && grep -rn "511" system_manifest.json README.md CLAUDE.md AGENTS.md llms.txt index.html CONTEXT_SNAPSHOT.md ppa/README_PPA.md paper.md reports/known_faults.yaml CONTRIBUTING.md UPDATE_PROTOCOL.md | grep -v "CLAIMS_DRAFT\|canonical_state" | head -20; echo "EXIT: $?"</automated>
  </verify>
  <done>Zero occurrences of 511 as a test count in any target file. All show 526. Branch fix/readme-mechanicus created with counter sync commit.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md with Mechanicus theme</name>
  <files>README.md</files>
  <action>
Rewrite README.md to be comprehensive and epic with Mechanicus (Warhammer 40K Adeptus Mechanicus) theme. Keep ALL existing technical content but restructure and expand.

**Structure (top to bottom):**

1. **Title and badges** -- Keep all existing badges but update test count badge to 526. Add a tagline like: "The Omnissiah's Protocol for Computational Truth"

2. **Mechanicus Mission Statement** (NEW) -- 3-5 lines. Theme: "In the grim darkness of unverifiable computation, one protocol stands between truth and fabrication." The machine spirit demands proof. Every claim verified. Every hash chained. No trust required. Only evidence.

3. **THE MECHANICUS PARALLEL table** (NEW):

| The Mechanicus | MetaGenesis Core |
|---|---|
| The Omnissiah | The Protocol (mg.py) |
| The Inquisition | steward_audit.py (governance enforcement) |
| The Litany of Protection | 5-layer verification (SHA-256 + semantic + step chain + signing + temporal) |
| The Machine Spirit | execution_trace + trace_root_hash |
| Sacred Unguents | cryptographic hash chain binding steps |
| Tech-Priest | The solo founder building the protocol |
| The Forge World | 14 claims across 7 domains |
| Binary Cant | Ed25519 + HMAC-SHA256 signatures |
| Cogitator | NIST Beacon temporal commitment |
| The Omnissiah's Test | 526 adversarial tests -- ALL PASS |

4. **"What this is" section** -- Keep existing content.

5. **THE FOUNDER'S STORY** (NEW/EXPANDED from "Built with Claude"):
   - Built by ONE person: Yehor Bazhynov
   - Construction worker. Ukraine to Canada immigrant.
   - No computer science degree.
   - From zero to patent pending (USPTO #63/996,819) in 14 days.
   - Built with Claude (Anthropic) as primary dev tool.
   - 14 claims. 526 tests. 5 verification layers. 8 innovations.
   - Working construction full-time while building this.
   - This is what happens when determination meets AI-accelerated development in 2026.

6. **Five verification layers + two pillars** -- Keep ALL existing content verbatim.

7. **PROOF: THE ADVERSARIAL GAUNTLET** (NEW/EXPANDED):
   List actual test cert names with what they prove:
   - CERT-02: Semantic bypass -- strip evidence, recompute hashes. CAUGHT.
   - CERT-03: Step chain tamper -- change inputs, rebuild trace. CAUGHT.
   - CERT-04: Cross-claim chain -- break upstream anchor. CAUGHT.
   - CERT-05: 5-attack gauntlet -- strip, bit-flip, cross-domain, canary, anchor reversal. ALL CAUGHT.
   - CERT-06: 5 real-world scenarios. ALL CAUGHT.
   - CERT-07: Bundle signing -- 13 attack vectors on HMAC-SHA256. ALL CAUGHT.
   - CERT-08: Reproducibility -- 10 determinism proofs. ALL PASS.
   - CERT-09: Ed25519 attacks -- key manipulation, signature forging. ALL CAUGHT.
   - CERT-10: Temporal attacks -- backdating, beacon manipulation. ALL CAUGHT.
   - CERT-11: Coordinated multi-vector -- all 5 layers proven independently necessary. PROVEN.
   - CERT-12: Encoding attacks -- BOM injection, null bytes, homoglyphs, truncated JSON. ALL CAUGHT.

   End with: `python -m pytest tests/ -q` -> `526 passed`

8. **14 active verification claims table** -- Keep existing.

9. **7 domains -- one protocol** -- Keep existing.

10. **8 innovations** -- Keep existing.

11. **Try it in 5 minutes / How it works** -- Keep existing.

12. **Verification state** -- Update to 526.

13. **What MetaGenesis Core does NOT claim** -- Keep existing. IMPORTANT: keep "tamper-evident, not tamper-proof" phrasing exactly.

14. **Get started / License / AI agents section** -- Keep existing, update 511->526.

15. **Footer** -- Keep existing.

**CRITICAL RULES:**
- NEVER use "tamper-proof" except in the contrast "tamper-evident, not tamper-proof"
- NEVER use "blockchain" -- use "cryptographic hash chain"
- NEVER use "unforgeable"
- NEVER use "100% test success"
- Keep ALL existing technical detail -- this is additive, not reductive
- Mechanicus theme should be atmospheric but not cringe -- keep it dignified
- All test references must say 526, not 511

Commit: `git add README.md && git commit -m "feat: rewrite README.md with Mechanicus theme and founder story"`
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && grep -c "511" README.md && echo "FAIL: 511 still present" || echo "PASS: no 511"; grep -c "Omnissiah" README.md > /dev/null && echo "PASS: Mechanicus theme present" || echo "FAIL: no Mechanicus theme"; grep -c "tamper-proof" README.md | grep -q "^0$" && echo "PASS: no banned tamper-proof" || (grep "tamper-proof" README.md | grep -q "not tamper-proof" && echo "PASS: tamper-proof only in contrast" || echo "FAIL: banned term")</automated>
  </verify>
  <done>README.md has Mechanicus mission statement, parallel table, expanded founder story (construction worker, Ukraine->Canada, no CS degree, 14 days to patent), full PROOF section with all CERT names. All existing technical content preserved. Zero occurrences of 511. No banned terms used incorrectly.</done>
</task>

<task type="auto">
  <name>Task 3: Push branch and final verification</name>
  <files></files>
  <action>
1. Run final grep across ALL files in repo for stale 511 test count references (excluding .git, .planning, node_modules):
   `grep -rn "511 test\|511 pass\|Tests-511\|test_count.*511" --include="*.md" --include="*.json" --include="*.txt" --include="*.yaml" --include="*.html" . | grep -v ".git/" | grep -v ".planning/" | grep -v "CLAIMS_DRAFT.md" | grep -v "canonical_state.md"`
   Fix any remaining.

2. Run verification gates (do NOT commit if they fail):
   - `python scripts/check_stale_docs.py` (should pass or warn-only)

3. Push branch: `git push origin fix/readme-mechanicus`

4. Report the branch URL for PR creation.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && git log --oneline -3 && git branch --show-current</automated>
  </verify>
  <done>Branch fix/readme-mechanicus pushed to origin with both commits. Zero stale 511 references in target files. Ready for PR.</done>
</task>

</tasks>

<verification>
1. `grep -rn "511" system_manifest.json README.md CLAUDE.md AGENTS.md llms.txt index.html CONTEXT_SNAPSHOT.md ppa/README_PPA.md paper.md` returns zero matches
2. README.md contains "Omnissiah", "Mechanicus", founder story, CERT proof section
3. README.md does NOT contain banned terms (tamper-proof used standalone, blockchain, unforgeable)
4. All 12 index.html occurrences updated including JS counter animation target
5. Branch pushed to origin
</verification>

<success_criteria>
- All target files show 526 as test count with zero remaining 511 references
- README.md is comprehensive with Mechanicus theme, parallel table, founder story, adversarial proof section
- All existing README technical content preserved (badges, quick start, layers, claims, domains, innovations)
- Committed to fix/readme-mechanicus branch and pushed
- No sealed files modified
- No banned terms used
</success_criteria>

<output>
After completion, create `.planning/quick/260319-m2v-counter-sync-511-526-readme-mechanicus-r/260319-m2v-SUMMARY.md`
</output>
