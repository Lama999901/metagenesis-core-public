---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [CLAUDE.md, AGENTS.md]
autonomous: true
requirements: [QUICK-m5w]
must_haves:
  truths:
    - "CLAUDE.md v2.0 committed with MISSION, COMMERCIAL PRIORITY, 6-domain table, physical anchor hierarchy, diff correction, agent traps, no stale v0.6.0 content"
    - "AGENTS.md v0.8.0 committed with 608 tests, v0.8 MVP, PHYS-01/02, MISSION, agent_pr_creator"
    - "No banned terms present in either file"
  artifacts:
    - path: "CLAUDE.md"
      provides: "v2.0 agent context"
    - path: "AGENTS.md"
      provides: "v0.8.0 agent rules"
  key_links: []
---

<objective>
Commit CLAUDE.md v2.0 and AGENTS.md v0.8.0 to a new branch and push for PR.

Purpose: Both files are already updated on disk. This plan verifies no banned terms, creates a branch, commits, and pushes.
Output: Branch pushed with both files ready for PR.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify banned terms, branch, commit, and push CLAUDE.md v2.0 + AGENTS.md v0.8.0</name>
  <files>CLAUDE.md, AGENTS.md</files>
  <action>
1. Scan both files for banned terms. Grep for each of these exact strings (case-insensitive):
   - "tamper-proof" (allowed: "tamper-evident")
   - "blockchain" (allowed: "cryptographic hash chain")
   - "unforgeable"
   - "GPT-5"
   - "100% test success"
   - "595 tests" (outdated)
   - "601 tests" (outdated — allowed only in BANNED TERMS section as "do not use" reference)
   - "v0.7.0" (outdated — allowed only in BANNED TERMS section as "do not use" reference)
   If ANY banned term appears outside the BANNED TERMS reference section, STOP and fix before continuing.

2. Verify current branch situation. If already on a feature branch for this work, use it. Otherwise:
   ```bash
   git checkout -b fix/agent-context-v2
   ```

3. Stage and commit:
   ```bash
   git add CLAUDE.md AGENTS.md
   git commit -m "docs: update CLAUDE.md v2.0 and AGENTS.md v0.8.0

   CLAUDE.md v2.0: add MISSION, COMMERCIAL PRIORITY, 6-domain table,
   physical anchor hierarchy, diff correction, agent traps section,
   remove stale v0.6.0 content, update WHAT'S NEXT.

   AGENTS.md v0.8.0: fix 595->608, MVP v0.6->v0.8, add PHYS-01/02,
   MISSION section, agent_pr_creator in acceptance commands."
   ```

4. Push:
   ```bash
   git push origin fix/agent-context-v2
   ```
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -q "CLAUDE.md v2.0" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Both files committed on fix/agent-context-v2 branch and pushed to origin. No banned terms present outside reference section.</done>
</task>

</tasks>

<verification>
- git log -1 shows commit with both files
- git diff HEAD~1 --name-only shows CLAUDE.md and AGENTS.md
- No banned terms in either file (outside banned terms reference list)
</verification>

<success_criteria>
Branch fix/agent-context-v2 pushed to origin with CLAUDE.md v2.0 and AGENTS.md v0.8.0 committed.
</success_criteria>

<output>
After completion, create `.planning/quick/260331-m5w-commit-claude-md-v2-0-and-agents-md-v0-8/260331-m5w-SUMMARY.md`
</output>
