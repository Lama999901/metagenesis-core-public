---
phase: quick
plan: 260318-j50
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, test_mg_sign_hmac.json, EVOLUTION_LOG.md, CLAUDE_PROJECT_MASTER_v5.md, NEXT_CHAT_PRIMER.md, debug_t9.py, check_runner.py, final_sweep.py, fix_conflicts.ps1]
autonomous: true
requirements: []

must_haves:
  truths:
    - "test_mg_sign_hmac.json is not in git history, working tree, or untracked files"
    - "EVOLUTION_LOG.md is not tracked by git and not in working tree"
    - "All dev artifact files are removed from disk"
    - ".gitignore prevents future accidental commits of these files"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for dev artifacts and HMAC key file"
      contains: "test_mg_sign_hmac.json"
  key_links: []
---

<objective>
URGENT security fix: remove exposed HMAC signing key file and dev artifacts from repo and git history.

Purpose: test_mg_sign_hmac.json contains an HMAC signing key that must never be in a public repo. Dev artifact files (EVOLUTION_LOG.md, debug scripts, etc.) are cluttering the repo. EVOLUTION_LOG.md is the only file actually tracked by git. test_mg_sign_hmac.json was never committed but exists on disk.
Output: Clean repo with no dev artifacts and no signing key material in history.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

DISCOVERY NOTE (Level 0 — no research needed):
- .gitignore already contains ALL required entries (lines 93-114 cover test_mg_sign_hmac.json, EVOLUTION_LOG.md, CLAUDE_PROJECT_MASTER_v*.md, NEXT_CHAT_PRIMER.md, fix_conflicts.ps1, check_runner.py, final_sweep.py, debug_t9.py)
- EVOLUTION_LOG.md is the ONLY file tracked by git (committed in 4810372)
- test_mg_sign_hmac.json was NEVER committed to git — it is untracked
- All other files (CLAUDE_PROJECT_MASTER_v5.md, NEXT_CHAT_PRIMER.md, debug_t9.py, check_runner.py, final_sweep.py, fix_conflicts.ps1) exist on disk but are already gitignored and untracked
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify .gitignore and remove tracked file</name>
  <files>.gitignore</files>
  <action>
1. Verify .gitignore already contains all required entries. The current .gitignore (lines 93-114) already has:
   - test_mg_sign_hmac.json (line 104)
   - EVOLUTION_LOG.md (line 94)
   - CLAUDE_PROJECT_MASTER_v*.md (line 95, covers v5)
   - NEXT_CHAT_PRIMER.md (line 96)
   - fix_conflicts.ps1 (line 110)
   - check_runner.py (line 111)
   - final_sweep.py (line 112)
   - debug_t9.py (line 113)
   If any are missing, add them. Do NOT add *.json — too broad.

2. Remove EVOLUTION_LOG.md from git tracking (it IS tracked — committed in 4810372):
   ```bash
   git rm -f --cached EVOLUTION_LOG.md
   ```
   Note: --cached removes from index only; file stays on disk but will be gitignored.

3. Commit the removal:
   ```bash
   git commit -m "fix: remove EVOLUTION_LOG.md from tracking (already gitignored)"
   ```
  </action>
  <verify>
    <automated>git ls-files EVOLUTION_LOG.md | wc -l</automated>
    Output must be 0 (file no longer tracked).
  </verify>
  <done>EVOLUTION_LOG.md is untracked. .gitignore confirmed to have all required entries.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dev artifact files from disk</name>
  <files>test_mg_sign_hmac.json, EVOLUTION_LOG.md, CLAUDE_PROJECT_MASTER_v5.md, NEXT_CHAT_PRIMER.md, debug_t9.py, check_runner.py, final_sweep.py, fix_conflicts.ps1</files>
  <action>
Delete all dev artifact files from disk. These are all untracked/gitignored at this point:
```bash
rm -f test_mg_sign_hmac.json EVOLUTION_LOG.md CLAUDE_PROJECT_MASTER_v5.md NEXT_CHAT_PRIMER.md debug_t9.py check_runner.py final_sweep.py fix_conflicts.ps1
```
No git commit needed — these are untracked files being deleted from disk.

Verify with `git status` that no untracked dev artifacts remain.
  </action>
  <verify>
    <automated>ls test_mg_sign_hmac.json EVOLUTION_LOG.md CLAUDE_PROJECT_MASTER_v5.md NEXT_CHAT_PRIMER.md debug_t9.py check_runner.py final_sweep.py fix_conflicts.ps1 2>&1 | grep -c "No such file"</automated>
    Output should be 8 (all files gone).
  </verify>
  <done>All 8 dev artifact files removed from disk. Working tree is clean of dev artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Scrub EVOLUTION_LOG.md from git history</name>
  <files></files>
  <action>
IMPORTANT FINDING: test_mg_sign_hmac.json was NEVER committed to git history (confirmed via `git log --all --oneline -- test_mg_sign_hmac.json` returning empty). No history scrub needed for it.

However, EVOLUTION_LOG.md WAS committed (in 4810372). Scrub it from history:

1. Install git-filter-repo if not available:
   ```bash
   pip install git-filter-repo
   ```

2. Run filter-repo to remove EVOLUTION_LOG.md from all history:
   ```bash
   git filter-repo --path EVOLUTION_LOG.md --invert-paths --force
   ```

3. IMPORTANT: After history rewrite, the remote origin will be removed by filter-repo. Re-add it:
   ```bash
   git remote add origin https://github.com/Lama999901/metagenesis-core-public.git
   ```

4. Print a clear message: "History rewrite complete. A force-push is required: git push --force origin --all"
   DO NOT force-push — the user will handle that separately.

NOTE: If git-filter-repo fails or is unavailable, try BFG Repo Cleaner as fallback. If neither works, print clear instructions for the user.
  </action>
  <verify>
    <automated>git log --all --oneline -- EVOLUTION_LOG.md 2>&1 | wc -l</automated>
    Output must be 0 (no commits reference EVOLUTION_LOG.md).
  </verify>
  <done>EVOLUTION_LOG.md removed from all git history. User informed that force-push is required.</done>
</task>

</tasks>

<verification>
1. `git ls-files test_mg_sign_hmac.json EVOLUTION_LOG.md` returns empty (nothing tracked)
2. `ls test_mg_sign_hmac.json EVOLUTION_LOG.md 2>&1` shows "No such file" for both
3. `git log --all --oneline -- EVOLUTION_LOG.md` returns empty (scrubbed from history)
4. `git log --all --oneline -- test_mg_sign_hmac.json` returns empty (was never committed)
5. `grep test_mg_sign_hmac .gitignore` returns a match
</verification>

<success_criteria>
- No HMAC key material exists in git history or working tree
- No dev artifact files exist on disk
- .gitignore prevents future accidental commits
- User is informed about required force-push after history rewrite
</success_criteria>

<output>
After completion, create `.planning/quick/260318-j50-urgent-security-fix-remove-hmac-key-file/260318-j50-SUMMARY.md`
</output>
