---
phase: 10-coverage-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/scripts/test_check_stale_docs_main.py
  - tests/scripts/test_agent_coverage_run.py
  - tests/scripts/test_agent_evolve_self_analyze.py
autonomous: true
requirements: [COV-01, COV-02, COV-04]
must_haves:
  truths:
    - "check_stale_docs.py check_stale_docs() is exercised for PASS paths, FAIL paths, strict mode, and content validation"
    - "agent_coverage.py analyze() is exercised end-to-end including summary mode, missing coverage.json, zero-coverage detection, low-coverage, report writing, and cleanup"
    - "agent_evolve_self.py analyze() is exercised end-to-end including report generation, recommendation building, and summary mode"
  artifacts:
    - path: "tests/scripts/test_check_stale_docs_main.py"
      provides: "Dedicated tests for check_stale_docs main flow"
      min_lines: 80
    - path: "tests/scripts/test_agent_coverage_run.py"
      provides: "Tests for agent_coverage analyze() run() flow"
      min_lines: 80
    - path: "tests/scripts/test_agent_evolve_self_analyze.py"
      provides: "Tests for agent_evolve_self analyze() end-to-end"
      min_lines: 80
  key_links:
    - from: "tests/scripts/test_check_stale_docs_main.py"
      to: "scripts/check_stale_docs.py"
      via: "import and monkeypatch"
      pattern: "import check_stale_docs"
    - from: "tests/scripts/test_agent_coverage_run.py"
      to: "scripts/agent_coverage.py"
      via: "import and monkeypatch"
      pattern: "import agent_coverage"
    - from: "tests/scripts/test_agent_evolve_self_analyze.py"
      to: "scripts/agent_evolve_self.py"
      via: "import and monkeypatch"
      pattern: "import agent_evolve_self"
---

<objective>
Add dedicated test files for the three highest-impact coverage targets: check_stale_docs.py main flow, agent_coverage.py analyze(), and agent_evolve_self.py analyze(). These scripts have 12, 23, and 15 uncovered lines respectively -- covering them adds ~50 lines toward the 90% goal.

Purpose: Address COV-01 (check_stale_docs dedicated tests), COV-04 (agent_coverage run() from 20%), and COV-02 (agent_evolve_self analyze() branches).
Output: Three new test files with ~15-20 tests total.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/check_stale_docs.py
@scripts/agent_coverage.py
@scripts/agent_evolve_self.py
@tests/scripts/test_check_stale_docs_pure.py
@tests/scripts/test_check_stale_docs_extended.py
@tests/scripts/test_agent_coverage_pure.py
@tests/scripts/test_agent_coverage_analyze.py
@tests/scripts/test_agent_evolve_self_pure.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add check_stale_docs.py main-flow and edge-case tests</name>
  <files>tests/scripts/test_check_stale_docs_main.py</files>
  <behavior>
    - Test check_stale_docs() detects STALE files when tracked code changed but doc did not (mock git to return changed scripts/ files but AGENTS.md not in changed set)
    - Test check_stale_docs() marks CURRENT when doc file IS in the changed set
    - Test check_stale_docs() marks OK when neither doc nor tracked code changed
    - Test check_stale_docs() handles MISSING doc files gracefully (file in CRITICAL_FILES but not on disk)
    - Test check_stale_docs() strict=True returns False when stale files exist
    - Test check_stale_docs() strict=False returns False (all_clean=False) when content_stale is non-empty
    - Test check_stale_docs() returns True when everything is clean (no stale, no content issues)
    - Test check_stale_docs() content validation runs independently of git (content_stale populated even when no git changes)
  </behavior>
  <action>
    Create tests/scripts/test_check_stale_docs_main.py with ~8 tests targeting check_stale_docs() main function (lines 371-465). Per COV-01, this is the first dedicated test file for the main orchestration flow.

    Existing test files cover: check_content (pure), files_in_tracked_paths (pure), get_last_merge_commit (mocked), get_files_changed_since (mocked), and a few check_stale_docs() smoke tests. The GAP is: no tests exercise the STALE/CURRENT/OK classification loop (lines 409-440) or the interaction between git-based staleness and content-based staleness.

    Strategy: Mock get_last_merge_commit, get_files_changed_since, and run() to control git state. Use tmp_path with patch("check_stale_docs.REPO_ROOT") to create/omit doc files and control content checks. Patch CRITICAL_FILES and CONTENT_CHECKS to use a small controlled set instead of the full 40+ file list.

    Each test should:
    1. Set up tmp_path with specific doc files (or omit them)
    2. Mock git functions to return controlled changed-file sets
    3. Call check_stale_docs(strict=...) and assert return value
    4. Optionally check capsys output for expected status markers

    Follow existing pattern from test_check_stale_docs_extended.py (patch.object on module functions).
  </action>
  <verify>
    <automated>python -m pytest tests/scripts/test_check_stale_docs_main.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. check_stale_docs() STALE/CURRENT/OK classification, strict mode, and content+git interaction paths are covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add agent_coverage.py and agent_evolve_self.py analyze() tests</name>
  <files>tests/scripts/test_agent_coverage_run.py, tests/scripts/test_agent_evolve_self_analyze.py</files>
  <behavior>
    agent_coverage analyze():
    - Test analyze() with corrupt coverage.json returns 1
    - Test analyze() report includes "Files Below 50%" section when applicable
    - Test analyze() generates suggestions for zero-coverage functions not in AGENT_TASKS.md
    - Test analyze() report includes "No zero-coverage" message when all functions are covered
    - Test analyze() --summary mode prints compact one-liner with coverage percentage

    agent_evolve_self analyze():
    - Test analyze() produces report with Agent Report History section
    - Test analyze() includes handler analysis (shallow/complex/ok verdicts)
    - Test analyze() includes recommendations from unaddressed patterns
    - Test analyze() includes recurring themes when headers repeat across reports
    - Test analyze() --summary mode prints compact one-liner
    - Test analyze() with no reports/patterns/handlers produces "no critical issues" recommendation
    - Test analyze() report includes report frequency warning when max gap > 7 days
  </behavior>
  <action>
    Create two test files targeting the analyze() orchestration functions per COV-04 and COV-02.

    **tests/scripts/test_agent_coverage_run.py** (~5 tests):
    Target uncovered branches in analyze() (lines 142-324). Existing test_agent_coverage_analyze.py covers 10 cases but misses: corrupt JSON handling, "Files Below 50%" report section, "No zero-coverage" report branch, suggestion generation with deduplication details. Mock REPO_ROOT to tmp_path, create fake coverage.json, source files, and AGENT_TASKS.md. Patch ac.run() to avoid real pytest execution.

    **tests/scripts/test_agent_evolve_self_analyze.py** (~7 tests):
    Target uncovered branches in analyze() (lines 209-375). Existing test_agent_evolve_self_pure.py covers sub-functions but never calls analyze() itself. Mock REPO_ROOT to tmp_path with fake reports/ directory containing AGENT_REPORT_*.md files, .agent_memory/patterns.json, and scripts/agent_research.py. Patch run() to avoid subprocess calls. Test:
    - Full flow with reports, patterns, and handlers
    - Summary mode output
    - Empty state (no reports, no patterns)
    - Report with recurring themes
    - Report frequency warning path (gap > 7 days between reports)

    Both files follow the established pattern: patch REPO_ROOT to tmp_path, patch run()/subprocess, use capsys for output assertions.
  </action>
  <verify>
    <automated>python -m pytest tests/scripts/test_agent_coverage_run.py tests/scripts/test_agent_evolve_self_analyze.py -v --tb=short 2>&1 | tail -25</automated>
  </verify>
  <done>All tests pass. agent_coverage analyze() run() path covered beyond 20%. agent_evolve_self analyze() orchestration and report generation branches covered.</done>
</task>

</tasks>

<verification>
Run all new and existing tests together to ensure zero regressions:
```bash
python -m pytest tests/scripts/test_check_stale_docs_main.py tests/scripts/test_agent_coverage_run.py tests/scripts/test_agent_evolve_self_analyze.py tests/scripts/test_check_stale_docs_pure.py tests/scripts/test_check_stale_docs_extended.py tests/scripts/test_agent_coverage_pure.py tests/scripts/test_agent_coverage_analyze.py tests/scripts/test_agent_evolve_self_pure.py -v --tb=short
```
All tests pass with zero failures.
</verification>

<success_criteria>
- tests/scripts/test_check_stale_docs_main.py exists with 8+ tests exercising the main check_stale_docs() flow
- tests/scripts/test_agent_coverage_run.py exists with 5+ tests covering analyze() branches
- tests/scripts/test_agent_evolve_self_analyze.py exists with 7+ tests covering analyze() end-to-end
- All existing 1634 tests still pass (zero regressions)
- Combined new tests add coverage for ~50 previously uncovered lines
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-hardening/10-01-SUMMARY.md`
</output>
