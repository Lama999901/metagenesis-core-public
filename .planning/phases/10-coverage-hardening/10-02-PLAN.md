---
phase: 10-coverage-hardening
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - tests/scripts/test_agent_research_write_report.py
  - tests/scripts/test_agent_diff_review_main.py
  - tests/scripts/test_agent_pr_creator_detectors.py
  - tests/scripts/test_agent_learn_commands.py
autonomous: true
requirements: [COV-03, COV-05]
must_haves:
  truths:
    - "agent_research.py write_report() and main() branches are covered by tests"
    - "agent_diff_review.py review_file() and main() branches are covered by tests"
    - "agent_pr_creator.py detect_stale_counters(), detect_manifest_sync(), and main() are covered"
    - "agent_learn.py recall(), brief(), and stats() commands are covered"
    - "Overall test coverage reaches 90%+ excluding deep_verify.py"
  artifacts:
    - path: "tests/scripts/test_agent_research_write_report.py"
      provides: "Tests for write_report() and main() flow"
      min_lines: 60
    - path: "tests/scripts/test_agent_diff_review_main.py"
      provides: "Tests for review_file() and main() flow"
      min_lines: 60
    - path: "tests/scripts/test_agent_pr_creator_detectors.py"
      provides: "Tests for detect_stale_counters, detect_manifest_sync, main"
      min_lines: 60
    - path: "tests/scripts/test_agent_learn_commands.py"
      provides: "Tests for recall, brief, stats commands"
      min_lines: 50
  key_links:
    - from: "tests/scripts/test_agent_research_write_report.py"
      to: "scripts/agent_research.py"
      via: "import and monkeypatch"
      pattern: "import agent_research"
    - from: "tests/scripts/test_agent_diff_review_main.py"
      to: "scripts/agent_diff_review.py"
      via: "import and monkeypatch"
      pattern: "import agent_diff_review"
    - from: "tests/scripts/test_agent_pr_creator_detectors.py"
      to: "scripts/agent_pr_creator.py"
      via: "import and monkeypatch"
      pattern: "import agent_pr_creator"
    - from: "tests/scripts/test_agent_learn_commands.py"
      to: "scripts/agent_learn.py"
      via: "import and monkeypatch"
      pattern: "import agent_learn"
---

<objective>
Cover remaining uncovered lines across agent_research.py, agent_diff_review.py, agent_pr_creator.py, and agent_learn.py to push overall coverage past 90%. These four scripts contribute ~62 uncovered lines combined. Covering ~100 lines total (with Plan 01) exceeds the 145-line target for 90%.

Purpose: Address COV-03 (agent_research write_report/branches) and COV-05 (90%+ overall). These are the remaining coverage gaps after Plan 01.
Output: Four new test files with ~20 tests total.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-coverage-hardening/10-01-SUMMARY.md
@scripts/agent_research.py
@scripts/agent_diff_review.py
@scripts/agent_pr_creator.py
@scripts/agent_learn.py
@tests/scripts/test_agent_research_pure.py
@tests/scripts/test_agent_research_handlers.py
@tests/scripts/test_agent_diff_review_pure.py
@tests/scripts/test_agent_diff_review_mock.py
@tests/scripts/test_agent_pr_creator_pure.py
@tests/test_agent_pr_creator.py
@tests/scripts/test_agent_learn_pure.py
@tests/scripts/test_agent_learn_extended.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add agent_research.py and agent_diff_review.py uncovered-branch tests</name>
  <files>tests/scripts/test_agent_research_write_report.py, tests/scripts/test_agent_diff_review_main.py</files>
  <behavior>
    agent_research write_report:
    - Test write_report() creates report file in reports/ with correct filename pattern
    - Test write_report() includes task title, description, priority, and findings in content
    - Test write_report() returns Path to created report
    - Test main() with no AGENT_TASKS.md calls sys.exit(1) -- mock sys.exit
    - Test main() with no PENDING tasks calls sys.exit(0) -- mock sys.exit

    agent_diff_review:
    - Test review_file() detects removed public function (mock get_old_source)
    - Test review_file() detects signature change in existing function
    - Test review_file() detects lost return statement
    - Test review_file() detects missing trace_root_hash in backend/progress/ file with _hash_step
    - Test main() returns 0 when no changed files
    - Test main() summary mode prints DIFF_PASS when clean
  </behavior>
  <action>
    Create two test files targeting uncovered branches. Per COV-03 and COV-05.

    **tests/scripts/test_agent_research_write_report.py** (~5 tests):
    Target write_report() (lines 2265-2281) which has zero dedicated tests. Also target main() entry paths (lines 2506-2540) -- mock execute_task, write_report, mark_task_done, generate_tasks, generate_coverage_tasks, weekly_report, and subprocess.run to avoid side effects. Test the "no tasks file" and "no pending" early-exit branches.

    Strategy: patch REPO_ROOT to tmp_path, create minimal AGENT_TASKS.md. For write_report, pass a task dict and findings string, verify file creation and content. For main, mock all downstream functions and verify dispatch flow.

    **tests/scripts/test_agent_diff_review_main.py** (~6 tests):
    Target review_file() regression-detection branches (lines 85-137) and main() flow (lines 140-178). Existing tests cover extract_structure() but NOT review_file() or main(). Those are the 13 uncovered lines.

    Strategy for review_file: Create tmp_path files, mock get_old_source() to return old Python source with a function, then create current version with that function removed/signature changed/return removed. For claim files, include _hash_step but omit trace_root_hash. For main: mock get_changed_py_files to return empty list (PASS) and to return files with issues (FAIL).
  </action>
  <verify>
    <automated>python -m pytest tests/scripts/test_agent_research_write_report.py tests/scripts/test_agent_diff_review_main.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. write_report file creation verified. main() entry branches covered. review_file() regression detection (removed functions, signature changes, lost returns, hash chain) covered. main() PASS/FAIL/summary paths covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add agent_pr_creator.py and agent_learn.py uncovered-branch tests</name>
  <files>tests/scripts/test_agent_pr_creator_detectors.py, tests/scripts/test_agent_learn_commands.py</files>
  <behavior>
    agent_pr_creator:
    - Test detect_stale_counters() returns stale=True when manifest count != actual (mock subprocess to return pytest output)
    - Test detect_stale_counters() returns stale=False when counts match
    - Test detect_stale_counters() dry_run=True does not call _auto_fix
    - Test detect_manifest_sync() returns synced=True when versions match (mock git describe)
    - Test detect_manifest_sync() returns synced=False when versions differ
    - Test main() prints "No auto-pr needed" when all detectors pass

    agent_learn:
    - Test recall() prints etalon and recent sessions from knowledge base
    - Test recall() prints "No sessions recorded" when KB is empty
    - Test brief() prints one-line summary with session count and pattern count
    - Test stats() prints total sessions, clean sessions, total issues, trend
    - Test stats() prints "No sessions yet" when KB is empty
  </behavior>
  <action>
    Create two test files for the remaining coverage gaps. Per COV-05.

    **tests/scripts/test_agent_pr_creator_detectors.py** (~6 tests):
    Target detect_stale_counters() (lines 33-60, 13 uncovered lines) and detect_manifest_sync() (lines 141-159). Existing test files cover detect_forbidden_terms and detect_coverage_drop but NOT stale counters or manifest sync. Strategy: Create tmp_path with system_manifest.json, mock subprocess.run to return fake pytest output (e.g., "1634 passed") and fake git describe output. Patch REPO_ROOT. Test both stale and non-stale scenarios. Also test main() output path where all detectors pass.

    For detect_stale_counters: write system_manifest.json with test_count=1634, mock subprocess.run to return "1634 passed" for matching case and "1700 passed" for stale case. Use dry_run=True to avoid git branch creation.

    For detect_manifest_sync: write system_manifest.json with version="0.9.0", mock subprocess to return git tag "0.9.0" (synced) or "0.8.0" (out of sync).

    **tests/scripts/test_agent_learn_commands.py** (~5 tests):
    Target recall() (lines 283-322, ~8 uncovered lines), brief() (lines 325-331), and stats() (lines 335-356, ~10 uncovered lines). Existing tests cover load_kb, save_kb, load_patterns, save_patterns, scan_file_for_stale, check_critical_files, observe, _write_lessons_log but NOT recall/brief/stats.

    Strategy: Patch KB_FILE and PATTERNS_FILE to tmp_path files. Pre-populate with known session data and pattern data. Call recall(), brief(), stats() and assert capsys output contains expected strings (session count, pattern count, etalon version, trend).
  </action>
  <verify>
    <automated>python -m pytest tests/scripts/test_agent_pr_creator_detectors.py tests/scripts/test_agent_learn_commands.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. detect_stale_counters and detect_manifest_sync covered. recall/brief/stats commands covered. Combined with Plan 01, total new coverage exceeds 145 lines needed for 90%.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm zero regressions and check coverage:
```bash
python -m pytest tests/ -q --tb=line 2>&1 | tail -5
```
All tests pass. Then run coverage check:
```bash
python -m pytest tests/ --cov=backend --cov=scripts --cov-report=term-missing -q --tb=no 2>&1 | grep "TOTAL"
```
Overall coverage line shows 90%+ (excluding deep_verify.py which is at 0%).
</verification>

<success_criteria>
- tests/scripts/test_agent_research_write_report.py exists with 5+ tests
- tests/scripts/test_agent_diff_review_main.py exists with 6+ tests
- tests/scripts/test_agent_pr_creator_detectors.py exists with 6+ tests
- tests/scripts/test_agent_learn_commands.py exists with 5+ tests
- All existing tests still pass (zero regressions)
- Overall coverage reaches 90%+ (excluding deep_verify.py)
</success_criteria>

<output>
After completion, create `.planning/phases/10-coverage-hardening/10-02-SUMMARY.md`
</output>
