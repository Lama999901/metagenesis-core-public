---
phase: 04-adversarial-proofs-and-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - system_manifest.json
  - index.html
  - README.md
  - AGENTS.md
  - llms.txt
  - CONTEXT_SNAPSHOT.md
  - scripts/deep_verify.py
  - reports/scientific_claim_index.md
  - paper.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04

must_haves:
  truths:
    - "User-visible innovation counter in index.html, README, and other docs reads 7 (was 6). system_manifest.json verified_innovations array contains 8 entries including temporal_commitment_nist_beacon."
    - "All counters across 6+ files reflect v0.4.0 state: 5 layers, correct test count"
    - "system_manifest.json protocol version is v0.4"
    - "deep_verify Test 7 passes with updated counter assertions"
    - "scientific_claim_index.md documents Ed25519 signing and temporal commitment capabilities"
    - "paper.md references Innovation #7 (Temporal Commitment)"
  artifacts:
    - path: "system_manifest.json"
      provides: "Protocol version bump and counter updates"
      contains: "v0.4"
    - path: "index.html"
      provides: "Updated counters in 11+ locations"
      contains: ">5<"
    - path: "reports/scientific_claim_index.md"
      provides: "New capability entries"
      contains: "temporal"
  key_links:
    - from: "scripts/deep_verify.py"
      to: "system_manifest.json"
      via: "Test 7 asserts protocol version and counter values"
      pattern: "manifest.*protocol"
    - from: "scripts/deep_verify.py"
      to: "index.html"
      via: "Test 7 asserts HTML counter values"
      pattern: "html"
---

<objective>
Update all documentation counters and references to reflect v0.4.0 state, including system_manifest.json protocol version bump, index.html counters (11+ locations), and all secondary documentation files.

Purpose: Ensures all public-facing and internal documentation accurately reflects the v0.4.0 protocol with 5 layers, 7 innovations, and correct test count.
Output: All counters consistent across 6+ files, deep_verify Test 7 passing with new values.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-adversarial-proofs-and-polish/04-01-SUMMARY.md
@.planning/phases/04-adversarial-proofs-and-polish/04-02-SUMMARY.md

<interfaces>
<!-- Current counter values that need updating -->

From system_manifest.json (current state):
```json
{
  "version": "0.3.0",
  "protocol": "MetaGenesis Verification Protocol (MVP) v0.3",
  "verified_innovations": [
    "bidirectional_claim_coverage",
    "tamper_evident_semantic_verification",
    "policy_gate_immutable_anchors",
    "dual_mode_canary_pipeline",
    "step_chain_verification",
    "cross_claim_cryptographic_chain",
    "bundle_signing_hmac_sha256"
  ],
  "test_count": 295
}
```

Target values:
- version: "0.4.0"
- protocol: "MetaGenesis Verification Protocol (MVP) v0.4"
- verified_innovations: add ONLY `"temporal_commitment_nist_beacon"` (making 8 total array entries). Do NOT add `"ed25519_asymmetric_signing"` as a separate entry -- Ed25519 was the upgrade path for Innovation #6 (bundle_signing_hmac_sha256), not a new innovation number. The user-visible innovation count goes from 6 to 7.
- test_count: DYNAMIC -- run `python -m pytest tests/ -q` and extract count

From scripts/deep_verify.py Test 7 (current assertions that must be updated):
```python
assert ">14<" in html  # claims count -- stays 14
assert ">3<" in html   # layers -- CHANGE to ">5<"
assert ">7<" in html   # domains -- stays 7
assert "v0.2" in manifest["protocol"]  # CHANGE to "v0.4"
```

From CLAUDE.md (counter update guidance):
- index.html has 11+ places with counters INCLUDING prose text
- Use PowerShell batch replace per BUG 7 guidance
- Layers: 3 -> 5
- Innovations: 6 -> 7 (user-visible count in HTML and docs)
- Test count: 295 -> actual dynamic count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Counter updates across all files + deep_verify Test 7 fix</name>
  <files>
    system_manifest.json
    index.html
    README.md
    AGENTS.md
    llms.txt
    CONTEXT_SNAPSHOT.md
    scripts/deep_verify.py
  </files>
  <read_first>
    - system_manifest.json (current values: version, protocol, verified_innovations, test_count)
    - index.html (search for ">3<", ">295<", "3 verification", "6 innovation" -- all counter locations)
    - README.md (search for counter values: "295", "3 layer", "3 verification", "6 innovation")
    - AGENTS.md (search for counter values)
    - llms.txt (search for counter values)
    - CONTEXT_SNAPSHOT.md (search for counter values)
    - scripts/deep_verify.py (Test 7 assertions at approx lines 190-203 -- must update ">3<" to ">5<" and "v0.2" to "v0.4")
  </read_first>
  <action>
**Step 0: Determine actual test count.**
Run `python -m pytest tests/ -q --tb=no` and extract the "N passed" number. Use this value for all counter updates.

**Step 1: Update system_manifest.json.**
- Change `"version"` from `"0.3.0"` to `"0.4.0"`
- Change `"protocol"` from `"MetaGenesis Verification Protocol (MVP) v0.3"` to `"MetaGenesis Verification Protocol (MVP) v0.4"`
- Add ONLY `"temporal_commitment_nist_beacon"` to `"verified_innovations"` array (making 8 total array entries). Do NOT add `"ed25519_asymmetric_signing"` -- Ed25519 was the upgrade path for Innovation #6 (bundle_signing_hmac_sha256 already present), not a new innovation number. Per user decision: "Update innovations: 6 -> 7".
- Change `"test_count"` from `295` to the actual dynamic count from Step 0

**Step 2: Update index.html (11+ locations).**
Counter changes needed:
- Layers: Replace all instances of `>3<` (layer count) with `>5<` -- but be careful to only replace the layer counter, not other ">3<" occurrences. Search for context like "layers" or "verification layers".
- Test count: Replace `>295<` with `>{actual_count}<` and any prose "295 tests" with "{actual_count} tests"
- Innovations: Update user-visible innovation counter from 6 to 7
- Version references: Update v0.3 to v0.4 where it appears as version
- "3 verification layers" in prose text -> "5 verification layers"
- "3-layer" in prose -> "5-layer"

Use PowerShell batch replace for index.html per CLAUDE.md BUG 7 guidance. Read the file first and identify ALL counter locations before making changes.

**Step 3: Update README.md.**
Search for and update:
- "295" -> actual test count (in context of tests)
- "3 verification layers" or "3-layer" -> "5 verification layers" / "5-layer"
- "6 innovations" -> "7 innovations"
- Version references v0.3 -> v0.4

**Step 4: Update AGENTS.md.**
Same counter updates as README.md. Search for "295", "3 layer", "3 verification", version references.

**Step 5: Update llms.txt.**
Same counter pattern. Search and replace test counts, layer counts, version.

**Step 6: Update CONTEXT_SNAPSHOT.md.**
Same counter pattern. Update all stale numbers.

**Step 7: Update deep_verify.py Test 7 assertions.**
In the Test 7 block (around lines 190-203):
- Change `assert ">3<" in html` to `assert ">5<" in html` with updated comment `"layers not 5 in HTML"`
- Change `assert "v0.2" in manifest["protocol"]` to `assert "v0.4" in manifest["protocol"]` with updated comment
- Update the print line: `print(f"  {OK} site: 14 claims, {manifest_tests} tests, 5 layers, 7 domains")`
- Update: `print(f"  {OK} system_manifest: 14 claims, {manifest_tests} tests, protocol v0.4")`

**IMPORTANT: Do NOT modify any sealed files listed in CLAUDE.md.** Do NOT touch steward_audit.py, mg_policy_gate_policy.json, canonical_state.md.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python scripts/deep_verify.py</automated>
  </verify>
  <acceptance_criteria>
    - system_manifest.json contains `"version": "0.4.0"`
    - system_manifest.json contains `"v0.4"` in protocol field
    - system_manifest.json contains `"temporal_commitment_nist_beacon"` in verified_innovations
    - system_manifest.json does NOT contain `"ed25519_asymmetric_signing"` as a separate innovation entry
    - system_manifest.json verified_innovations array has exactly 8 entries (7 existing + 1 new)
    - system_manifest.json test_count matches actual `pytest tests/ -q` count
    - index.html contains `>5<` (layer count)
    - index.html does NOT contain `>3<` as a layer count (may still appear in other contexts)
    - scripts/deep_verify.py contains `">5<" in html`
    - scripts/deep_verify.py contains `"v0.4" in manifest["protocol"]`
    - scripts/deep_verify.py does NOT contain `">3<" in html`
    - scripts/deep_verify.py does NOT contain `"v0.2" in manifest`
    - `python scripts/deep_verify.py` exits 0 with "ALL 13 TESTS PASSED"
  </acceptance_criteria>
  <done>All counters updated across 6+ files, deep_verify Test 7 passes with v0.4.0 values, no stale v0.3.0 counter references remain. User-visible innovation count is 7, manifest array has 8 entries.</done>
</task>

<task type="auto">
  <name>Task 2: scientific_claim_index.md and paper.md updates</name>
  <files>
    reports/scientific_claim_index.md
    paper.md
  </files>
  <read_first>
    - reports/scientific_claim_index.md (current state, structure, existing entries)
    - paper.md (current innovation references, bibliography structure, existing citations)
  </read_first>
  <action>
**Step 1: Update reports/scientific_claim_index.md (DOCS-03).**
Add entries documenting the new v0.4.0 capabilities:
- Ed25519 asymmetric signing (Innovation #6 upgrade from HMAC-only to dual HMAC+Ed25519)
  - What it does: Allows bundle signing with Ed25519 keys for third-party auditor verification
  - Files: scripts/mg_ed25519.py, scripts/mg_sign.py
  - Tests: tests/steward/test_ed25519.py, tests/steward/test_cert07_bundle_signing.py, tests/steward/test_cert09_ed25519_attacks.py
- Temporal Commitment (Innovation #7, Layer 5)
  - What it does: Proves WHEN a bundle was signed using NIST Randomness Beacon 2.0 pre-commitment scheme
  - Files: scripts/mg_temporal.py
  - Tests: tests/steward/test_temporal.py, tests/steward/test_cert10_temporal_attacks.py
- 5-Layer independence proof
  - What it does: Proves each verification layer catches attacks the other 4 miss
  - Files: tests/steward/test_cert_5layer_independence.py
- Update any version references from v0.3 to v0.4
- Update layer count references from 3 to 5
- Update innovation count if mentioned

**Step 2: Update paper.md (DOCS-04).**
Read the current paper.md structure first. Add references to:
- Innovation #7: Temporal Commitment with NIST Beacon pre-commitment scheme
- 5-layer verification architecture (expanded from 3-layer)
- Ed25519 asymmetric signing capability
- Updated test count and layer count in any statistical claims

Do NOT rewrite the paper -- make targeted additions. Add to existing sections where innovations or verification layers are discussed. If there's a bibliography section, add relevant references only if the paper structure supports it.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python -c "import pathlib; sci=pathlib.Path('reports/scientific_claim_index.md').read_text(); paper=pathlib.Path('paper.md').read_text(); assert 'temporal' in sci.lower(), 'sci missing temporal'; assert 'ed25519' in sci.lower(), 'sci missing ed25519'; assert '5' in sci and 'layer' in sci.lower(), 'sci missing 5 layers'; print('DOCS-03 and DOCS-04 checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - reports/scientific_claim_index.md contains "temporal" (case-insensitive)
    - reports/scientific_claim_index.md contains "ed25519" or "Ed25519"
    - reports/scientific_claim_index.md contains "5" and "layer" in context of verification layers
    - paper.md contains reference to temporal commitment or Innovation #7
    - paper.md contains reference to 5-layer or five-layer architecture
  </acceptance_criteria>
  <done>scientific_claim_index.md documents Ed25519 and temporal commitment capabilities, paper.md references Innovation #7 and 5-layer architecture</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/999ye/Downloads/metagenesis-core-public
python scripts/deep_verify.py
python scripts/steward_audit.py
python -m pytest tests/ -q --tb=short
```
All 13 deep_verify tests pass (including Test 7 with updated counters). Steward audit passes. Full test suite green.
</verification>

<success_criteria>
- All counters consistent: 5 layers, 7 user-visible innovations (8 manifest entries), correct test count across all 6+ files
- system_manifest.json protocol = "v0.4"
- deep_verify Test 7 passes with v0.4.0 assertions
- scientific_claim_index.md documents new capabilities
- paper.md references Innovation #7
- No sealed files modified
- Steward audit PASS
</success_criteria>

<output>
After completion, create `.planning/phases/04-adversarial-proofs-and-polish/04-03-SUMMARY.md`
</output>
