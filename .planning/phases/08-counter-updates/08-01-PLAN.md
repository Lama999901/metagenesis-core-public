---
phase: 08-counter-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - system_manifest.json
  - CLAUDE.md
  - README.md
  - AGENTS.md
  - llms.txt
  - CONTEXT_SNAPSHOT.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "system_manifest.json test_count equals actual pytest count (511)"
    - "system_manifest.json version is 0.5.0"
    - "All documentation files reference 511 tests (not 389, 295, or 270)"
    - "All version references updated from v0.3.0/v0.4.0 to v0.5.0 where appropriate"
    - "CLAUDE.md counters match system_manifest.json"
    - "GOV-03 governance meta-tests pass with updated counters"
  artifacts:
    - path: "system_manifest.json"
      provides: "Authoritative test count and version"
      contains: "\"test_count\": 511"
    - path: "CLAUDE.md"
      provides: "Primary AI context with correct counters"
      contains: "511 tests"
    - path: "README.md"
      provides: "Repository readme with correct badge count"
      contains: "511"
    - path: "AGENTS.md"
      provides: "Agent instructions with correct test count"
      contains: "511"
    - path: "llms.txt"
      provides: "LLM summary with correct counters"
      contains: "511"
    - path: "CONTEXT_SNAPSHOT.md"
      provides: "Live state snapshot with correct counters"
      contains: "511"
  key_links:
    - from: "tests/steward/test_stew08_documentation_drift.py"
      to: "system_manifest.json"
      via: "GOV-03 relational assertions"
      pattern: "manifest.*test_count"
---

<objective>
Update system_manifest.json with final v0.5.0 test count (511) and version (0.5.0), then propagate counters and version strings to all documentation files: CLAUDE.md, README.md, AGENTS.md, llms.txt, CONTEXT_SNAPSHOT.md.

Purpose: All documentation must reflect the actual v0.5.0 state so GOV-03 governance meta-tests pass and downstream agents/users see correct numbers.
Output: 6 files updated with consistent counters and version strings.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-counter-updates/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update system_manifest.json and all documentation counters</name>
  <files>system_manifest.json, CLAUDE.md, README.md, AGENTS.md, llms.txt, CONTEXT_SNAPSHOT.md</files>
  <read_first>
    - system_manifest.json (current state: test_count=389, version=0.4.0)
    - CLAUDE.md (has stale counters: "389 tests", "295 passing", "v0.3.0", "v0.4.0")
    - README.md (has "389 passing" badge and other references)
    - AGENTS.md (has "389 passed")
    - llms.txt (has "389 passed", "v0.3.0")
    - CONTEXT_SNAPSHOT.md (has "389 passing", "v0.4.0")
    - tests/steward/test_stew08_documentation_drift.py (GOV-03 test to understand what it checks)
  </read_first>
  <action>
    1. Update system_manifest.json (source of truth):
       - Change "test_count": 389 to "test_count": 511
       - Change "version": "0.4.0" to "version": "0.5.0"
       - Change "protocol": "MetaGenesis Verification Protocol (MVP) v0.4" to "MetaGenesis Verification Protocol (MVP) v0.5"
       - Change "github_release": "v0.4.0" to "github_release": "v0.5.0"

    2. Update CLAUDE.md:
       - Line 4: "v0.4.0 LIVE | 14 claims | 389 tests" -> "v0.5.0 LIVE | 14 claims | 511 tests"
       - Line 16: "v0.4.0 LIVE" -> "v0.5.0 LIVE"
       - Line 44: "389 tests PASS" -> "511 tests PASS"
       - Line 85: "Tests:      295 passing" -> "Tests:      511 passing"
       - Line 88: "Release:    v0.3.0" -> "Release:    v0.5.0"
       - Line 243: "14/295/3/7 counters" -> "14/511/5/12 counters" (5 layers, 12 CERTs)
       - Line 347: "MetaGenesis Core v0.3.0" -> "MetaGenesis Core v0.5.0"
       - Also update "3 verification" references to "5 verification" where Layer count is mentioned

    3. Update README.md:
       - Line 8: badge "Tests-389%20passing" -> "Tests-511%20passing"
       - Line 275: "389 passed" -> "511 passed"
       - Line 337: "14 claims, 389 tests" -> "14 claims, 511 tests"

    4. Update AGENTS.md:
       - Line 107: "389 passed" -> "511 passed"

    5. Update llms.txt:
       - Line 101: "389 passed" -> "511 passed"
       - Line 107: "Tests: 389 passing" -> "Tests: 511 passing"
       - Line 117: "v0.3.0" -> "v0.5.0" in GitHub Release URL

    6. Update CONTEXT_SNAPSHOT.md:
       - Line 25: "389 passing" -> "511 passing"
       - Line 32: version references v0.4.0 -> v0.5.0
       - Line 94: "389 passed" -> "511 passed"
       - Line 107: "updated to 389" -> "updated to 511"

    IMPORTANT: Search each file for ALL occurrences of "389", "295", "270" and replace with "511". Search for "v0.3.0" and "v0.4.0" and update to "v0.5.0" where they refer to the current release (not historical references).
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python -c "import json; m=json.loads(open('system_manifest.json').read()); assert m['test_count']==511, f'got {m[\"test_count\"]}'; assert m['version']=='0.5.0', f'got {m[\"version\"]}'; print('manifest OK')" && python -m pytest tests/steward/test_stew08_documentation_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - system_manifest.json contains "test_count": 511
    - system_manifest.json contains "version": "0.5.0"
    - CLAUDE.md contains "511 tests" (not 389 or 295)
    - CLAUDE.md contains "v0.5.0 LIVE"
    - README.md contains "Tests-511" in badge
    - AGENTS.md contains "511 passed"
    - llms.txt contains "511 passing" and "511 passed"
    - CONTEXT_SNAPSHOT.md contains "511 passing"
    - No file contains stale "389 tests" or "295 passing" or "270 tests" references
    - python -m pytest tests/steward/test_stew08_documentation_drift.py -q exits 0
  </acceptance_criteria>
  <done>All 6 documentation files have consistent counters matching system_manifest.json test_count=511 and version=0.5.0, and GOV-03 governance meta-tests pass</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/steward/test_stew08_documentation_drift.py -q` -- all GOV-03 tests pass
2. `python scripts/steward_audit.py` -- STEWARD AUDIT: PASS
3. Grep for stale values: no occurrences of "389 tests", "295 passing", "270 tests" in updated files
</verification>

<success_criteria>
- system_manifest.json test_count is 511, version is 0.5.0
- All 6 documentation files updated with matching counters
- GOV-03 governance meta-tests pass
- Steward audit passes
- Zero stale counter references in updated files
</success_criteria>

<output>
After completion, create `.planning/phases/08-counter-updates/08-01-SUMMARY.md`
</output>
