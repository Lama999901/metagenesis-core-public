---
phase: 08-counter-updates
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - index.html
autonomous: false
requirements:
  - DOCS-01

must_haves:
  truths:
    - "index.html shows 511 tests everywhere (not 389 or 270)"
    - "index.html shows v0.5.0 version"
    - "index.html shows 5 layers in protocol section"
    - "index.html has CERT-11 row in crisis/proof section"
    - "index.html has CERT-12 row in crisis/proof section"
    - "GOV-03 governance meta-tests pass with index.html updated"
  artifacts:
    - path: "index.html"
      provides: "Site with updated counters, Layer 5, CERT-11/12 content"
      contains: "511"
  key_links:
    - from: "index.html"
      to: "system_manifest.json"
      via: "Counter consistency checked by GOV-03"
      pattern: "511"
---

<objective>
Update index.html with final v0.5.0 counters (511 tests, 5 layers), add Layer 5 Temporal Commitment to protocol section, add CERT-11 and CERT-12 rows to proof strip, and update stats/feature matrix.

Purpose: The project site must reflect v0.5.0 capabilities -- 5 verification layers, CERT-11/12 flagship proofs, and correct test counts.
Output: index.html fully updated for v0.5.0.
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-counter-updates/08-CONTEXT.md
@.planning/phases/08-counter-updates/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html counters, version, Layer 5, CERT-11/12 content</name>
  <files>index.html</files>
  <read_first>
    - index.html (full file -- understand all 11+ counter locations, protocol section structure, proof strip structure, stats section, compare section, JavaScript counter animation)
    - system_manifest.json (source of truth for counters after 08-01 updates)
    - tests/steward/test_cert11_coordinated_attack.py (CERT-11 thesis for crisis section content)
    - tests/steward/test_cert12_encoding_attacks.py (CERT-12 thesis for crisis section content)
    - scripts/mg_temporal.py (Layer 5 description for protocol section)
    - tests/steward/test_stew08_documentation_drift.py (GOV-03 test to understand index.html validation)
  </read_first>
  <action>
    **Counter Updates (all 11+ locations):**
    Replace ALL occurrences of "389" with "511" in index.html. Known locations:
    - Line 1509: "389 tests" in GitHub repo link -> "511 tests"
    - Line 1582: hero badge "389" -> "511"
    - Line 1626: hero proof value "389" -> "511"
    - Line 1652: stats "389" Tests Passing -> "511"
    - Line 1659: stats "389" Tests Passing -> "511"
    - Line 1756: "270 tests" -> "511 tests" (stale historical value)
    - Line 1925: overview stats "389" -> "511"
    - Line 2095: terminal output "389 passed" -> "511 passed"
    - Line 2225: stats section "389" -> "511"
    - Line 2480: feature list "389 tests" -> "511 tests"
    - Line 3315: JavaScript counter animation target ct(cn2, 389, ...) -> ct(cn2, 511, ...)

    **Version Updates:**
    Search for "v0.4.0" and "0.4.0" in index.html and update to "v0.5.0" / "0.5.0" for current release references.

    **Layer Count Updates:**
    Update layer count references from "3 layers" or "4 layers" to "5 layers" where they describe the verification stack. Update the JavaScript counter ct(cn3, ...) if it animates the layer count.

    **CERT Count Updates:**
    If there is a CERT file count (e.g., "7 CERT files" or "8 CERT"), update to 12 (CERT-01 through CERT-12).

    **Protocol Section - Add Layer 5:**
    Find the protocol/verification section where Layers 1-4 are listed. Add Layer 5 entry following the exact same HTML pattern as Layers 1-4:
    - Layer 5 -- Temporal Commitment
    - File: scripts/mg_temporal.py
    - Catches: backdated bundle (NIST Beacon)

    **Proof Strip - Add CERT-11 and CERT-12:**
    Find the proof strip section (around line 1738-1756) where test_cert02, test_cert03, etc. are listed. Add two new entries following the exact same HTML pattern:
    - CERT-11: "test_cert11 -- coordinated multi-vector attack CAUGHT"
      Link: tests/steward/test_cert11_coordinated_attack.py
    - CERT-12: "test_cert12 -- encoding attacks CAUGHT"
      Link: tests/steward/test_cert12_encoding_attacks.py

    **Stats Section:**
    Update all stats to reflect v0.5.0:
    - Tests: 511
    - Layers: 5
    - CERT files: 12
    - Claims: 14 (unchanged)

    **Compare/Feature Matrix:**
    If the compare section has a feature matrix, add rows for:
    - Layer 5 Temporal Commitment (NIST Beacon anchoring)
    - Coordinated multi-vector attack resistance (CERT-11)

    **Terminal Output Section:**
    Update the fake terminal output (around line 2095) from "389 passed" to "511 passed".

    IMPORTANT: Use careful find-and-replace. index.html is a large file with minified CSS/JS. Do NOT accidentally replace "389" inside unrelated CSS values or JavaScript. Context-check each replacement.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python -c "
t = open('index.html', encoding='utf-8').read()
import re
# Check no stale 389 remains
matches_389 = [m.start() for m in re.finditer(r'389', t)]
assert len(matches_389) == 0, f'Still found 389 at positions: {matches_389}'
# Check 511 appears multiple times
count_511 = t.count('511')
assert count_511 >= 10, f'Only {count_511} occurrences of 511, expected 10+'
# Check CERT-11 and CERT-12 present
assert 'cert11' in t.lower() or 'cert-11' in t.lower(), 'CERT-11 not found'
assert 'cert12' in t.lower() or 'cert-12' in t.lower(), 'CERT-12 not found'
print(f'index.html OK: {count_511} occurrences of 511, CERT-11/12 present')
" && python -m pytest tests/steward/test_stew08_documentation_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains zero occurrences of "389" as a test count
    - index.html contains zero occurrences of "270" as a test count
    - index.html contains "511" in at least 10 locations
    - index.html contains CERT-11 reference (test_cert11 link)
    - index.html contains CERT-12 reference (test_cert12 link)
    - index.html contains "Layer 5" or "Temporal Commitment" in protocol section
    - index.html JavaScript counter animation targets 511 (not 389)
    - python -m pytest tests/steward/test_stew08_documentation_drift.py -q exits 0
  </acceptance_criteria>
  <done>index.html has all 11+ counter locations updated to 511, version to v0.5.0, Layer 5 added to protocol section, CERT-11/12 rows added to proof strip, stats reflect 5 layers and 12 CERTs</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of updated site</name>
  <files>index.html</files>
  <action>
    Human visually verifies the updated index.html in a browser. All automated updates from Task 1 and Plan 08-01 are already complete. This checkpoint confirms the visual rendering is correct and no layout issues were introduced.
  </action>
  <verify>
    <automated>cd C:/Users/999ye/Downloads/metagenesis-core-public && python -m pytest tests/ -q && python scripts/steward_audit.py</automated>
  </verify>
  <what-built>Complete v0.5.0 counter updates across all documentation (Plan 08-01) and index.html content overhaul including Layer 5, CERT-11/12 rows, updated stats and feature matrix (Plan 08-02).</what-built>
  <how-to-verify>
    1. Open index.html in browser (file:// or local server)
    2. Check hero section shows "511 tests" and "v0.5.0"
    3. Scroll to protocol section -- confirm Layer 5 (Temporal Commitment) appears after Layer 4
    4. Scroll to proof strip -- confirm CERT-11 and CERT-12 rows appear
    5. Check stats section shows 511 tests, 5 layers, 14 claims
    6. Check compare/feature matrix includes Layer 5 and CERT-11
    7. Run: python -m pytest tests/ -q -- confirm all tests pass (511 passed)
    8. Run: python scripts/steward_audit.py -- confirm STEWARD AUDIT: PASS
  </how-to-verify>
  <done>User has visually confirmed index.html renders correctly with all v0.5.0 updates, and full test suite + steward audit pass</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `python -m pytest tests/steward/test_stew08_documentation_drift.py -q` -- all GOV-03 tests pass
2. `python scripts/steward_audit.py` -- STEWARD AUDIT: PASS
3. `python -m pytest tests/ -q` -- all 511 tests pass, zero regressions
4. No stale counter values (389, 295, 270) remain in any documentation file
</verification>

<success_criteria>
- index.html has 511 test count in all 11+ locations
- index.html has Layer 5 Temporal Commitment in protocol section
- index.html has CERT-11 and CERT-12 in proof strip
- All GOV-03 governance meta-tests pass
- Steward audit passes
- Full test suite passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/08-counter-updates/08-02-SUMMARY.md`
</output>
