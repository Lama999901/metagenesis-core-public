---
phase: 05-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/mg.py
  - tests/steward/test_cert03_step_chain_verify.py
autonomous: true
requirements:
  - CHAIN-02
  - CHAIN-03
  - CHAIN-04

must_haves:
  truths:
    - "_verify_semantic rejects a trace with misordered steps (1,3,2,4)"
    - "_verify_semantic rejects a trace with duplicate step numbers (1,2,2,4)"
    - "_verify_semantic rejects a trace with extra steps beyond 4 (5 steps)"
    - "_verify_semantic still accepts valid 4-step traces with sequential numbering"
  artifacts:
    - path: "scripts/mg.py"
      provides: "Step ordering and count validation in _verify_semantic"
      contains: "step_numbers != [1, 2, 3, 4]"
    - path: "tests/steward/test_cert03_step_chain_verify.py"
      provides: "Step ordering invariant tests"
      contains: "test_wrong_step_order_rejected"
  key_links:
    - from: "tests/steward/test_cert03_step_chain_verify.py"
      to: "scripts/mg.py"
      via: "from scripts.mg import _verify_semantic"
      pattern: "_verify_semantic"
---

<objective>
Add step ordering and count validation to `_verify_semantic` in scripts/mg.py, then add 3-4 tests to test_cert03 that prove misordered, duplicate, and extra-step traces are rejected.

Purpose: Without step ordering validation, an attacker could reorder step chain entries and _verify_semantic would not catch it. This is a prerequisite for CERT-11 coordinated attack proofs in Phase 7.

Output: Enhanced `_verify_semantic` with ~10 lines of step validation + 3-4 new tests in test_cert03
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md

<interfaces>
<!-- Current _verify_semantic step chain section (scripts/mg.py lines 225-256) -->
From scripts/mg.py (lines 241-256, the section to modify):
```python
if not isinstance(execution_trace, list) or len(execution_trace) == 0:
    msg = (f"Run artifact {run_rel} execution_trace "
           f"must be a non-empty list")
    return False, msg, [msg]
for step in execution_trace:
    h = step.get("hash", "")
    if not (isinstance(h, str) and len(h) == 64
            and all(c in "0123456789abcdef" for c in h)):
        msg = (f"Run artifact {run_rel} execution_trace "
               f"step {step.get('step')} has invalid hash")
        return False, msg, [msg]
last_hash = execution_trace[-1].get("hash", "")
if claimed_root != last_hash:
    msg = (f"Run artifact {run_rel} trace_root_hash does not match "
           f"final execution_trace step hash -- Step Chain broken")
    return False, msg, [msg]
```

From tests/steward/test_cert03_step_chain_verify.py (existing helpers):
```python
_VALID_HASH_A = "a" * 64
_VALID_HASH_B = "b" * 64
_VALID_HASH_C = "c" * 64
_VALID_HASH_D = "d" * 64

_VALID_TRACE = [
    {"step": 1, "name": "init_params",      "hash": _VALID_HASH_A},
    {"step": 2, "name": "generate_dataset", "hash": _VALID_HASH_B},
    {"step": 3, "name": "compute_metrics",  "hash": _VALID_HASH_C},
    {"step": 4, "name": "threshold_check",  "hash": _VALID_HASH_D},
]
_VALID_ROOT = _VALID_HASH_D

def _make_pack(tmp_path, execution_trace=None, trace_root_hash=None,
               omit_trace=False, omit_root=False) -> tuple:
    """Build a minimal fake pack directory with one claim (ML_BENCH-01)."""
```

From tests/steward/test_cert03_step_chain_verify.py (existing test class):
```python
class TestStepChainVerification:
    def test_valid_trace_root_hash_passes(self, tmp_path): ...
    def test_tampered_trace_root_hash_fails(self, tmp_path): ...
    def test_missing_trace_root_hash_when_trace_present_fails(self, tmp_path): ...
    def test_missing_trace_when_root_hash_present_fails(self, tmp_path): ...
    def test_invalid_step_hash_format_fails(self, tmp_path): ...
    def test_no_trace_fields_passes(self, tmp_path): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add step ordering and count validation to _verify_semantic in scripts/mg.py</name>
  <files>scripts/mg.py</files>
  <read_first>
    - scripts/mg.py (full file -- understand the _verify_semantic function context, especially lines 225-270)
    - CLAUDE.md (verify scripts/mg.py is "modify carefully" not SEALED)
  </read_first>
  <action>
Add step ordering and count validation to `_verify_semantic` in scripts/mg.py. Insert the following checks AFTER the existing non-empty list check (line 244) and BEFORE the per-step hash validation loop (line 245):

```python
# --- Step count and ordering validation ---
if len(execution_trace) != 4:
    msg = (f"Run artifact {run_rel} execution_trace "
           f"must have exactly 4 steps, got {len(execution_trace)}")
    return False, msg, [msg]
step_numbers = [s.get("step") for s in execution_trace]
if step_numbers != [1, 2, 3, 4]:
    msg = (f"Run artifact {run_rel} execution_trace "
           f"steps must be [1, 2, 3, 4], got {step_numbers}")
    return False, msg, [msg]
```

This adds exactly 2 checks:
1. `len(execution_trace) != 4` -- rejects traces with extra steps (CHAIN-04) or too few
2. `step_numbers != [1, 2, 3, 4]` -- rejects misordered steps like (1,3,2,4) (CHAIN-02) and duplicate step numbers like (1,2,2,4) (CHAIN-03)

IMPORTANT: scripts/mg.py is listed in CLAUDE.md as "modify carefully" (NOT sealed). This ~10 line change is the only production code modification in Phase 5. Do NOT modify any other function in mg.py.

After the change, the step chain validation section should flow:
1. Check execution_trace is non-empty list (existing)
2. Check exactly 4 steps (NEW)
3. Check step numbers are [1,2,3,4] (NEW)
4. Validate each step hash format (existing)
5. Check root hash matches last step hash (existing)
  </action>
  <verify>
    <automated>python -m pytest tests/steward/test_cert03_step_chain_verify.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - scripts/mg.py contains the string `must have exactly 4 steps`
    - scripts/mg.py contains the string `steps must be [1, 2, 3, 4]`
    - scripts/mg.py contains `step_numbers = [s.get("step") for s in execution_trace]`
    - All 6 existing test_cert03 tests still pass (zero regressions)
    - `python -m pytest tests/ -q 2>&1 | tail -3` shows no regressions in full suite
    - `python scripts/steward_audit.py` exits 0
  </acceptance_criteria>
  <done>_verify_semantic rejects traces with wrong count, wrong ordering, and duplicates. All existing tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ordering invariant tests to test_cert03</name>
  <files>tests/steward/test_cert03_step_chain_verify.py</files>
  <read_first>
    - tests/steward/test_cert03_step_chain_verify.py (current file with 6 tests and _make_pack helper)
    - scripts/mg.py (verify the new step ordering checks are in place from Task 1)
  </read_first>
  <behavior>
    - Test: test_wrong_step_order_rejected -- trace with steps (1,3,2,4) is rejected, error message contains "steps must be"
    - Test: test_duplicate_step_numbers_rejected -- trace with steps (1,2,2,4) is rejected, error message contains "steps must be"
    - Test: test_extra_steps_rejected -- trace with 5 steps (1,2,3,4,5) is rejected, error message contains "must have exactly 4 steps"
    - Test: test_fewer_steps_rejected -- trace with 3 steps (1,2,3) is rejected, error message contains "must have exactly 4 steps"
  </behavior>
  <action>
Add 4 new test methods to the existing `TestStepChainVerification` class in tests/steward/test_cert03_step_chain_verify.py. Add them AFTER the existing `test_no_trace_fields_passes` method.

First, add a 5th valid hash constant at the top of the file (after existing `_VALID_HASH_D`):
```python
_VALID_HASH_E = "e" * 64
```

Then add these 4 tests inside `TestStepChainVerification`:

```python
def test_wrong_step_order_rejected(self, tmp_path):
    """Trace with steps (1,3,2,4) must be rejected by _verify_semantic."""
    misordered = [
        {"step": 1, "name": "init_params",      "hash": _VALID_HASH_A},
        {"step": 3, "name": "compute_metrics",  "hash": _VALID_HASH_C},
        {"step": 2, "name": "generate_dataset", "hash": _VALID_HASH_B},
        {"step": 4, "name": "threshold_check",  "hash": _VALID_HASH_D},
    ]
    pack_dir, index_path = _make_pack(
        tmp_path, execution_trace=misordered, trace_root_hash=_VALID_HASH_D)
    ok, msg, errors = _verify_semantic(pack_dir, index_path)
    assert ok is False
    assert "steps must be" in msg

def test_duplicate_step_numbers_rejected(self, tmp_path):
    """Trace with duplicate step numbers (1,2,2,4) must be rejected."""
    duped = [
        {"step": 1, "name": "init_params",      "hash": _VALID_HASH_A},
        {"step": 2, "name": "generate_dataset", "hash": _VALID_HASH_B},
        {"step": 2, "name": "compute_metrics",  "hash": _VALID_HASH_C},
        {"step": 4, "name": "threshold_check",  "hash": _VALID_HASH_D},
    ]
    pack_dir, index_path = _make_pack(
        tmp_path, execution_trace=duped, trace_root_hash=_VALID_HASH_D)
    ok, msg, errors = _verify_semantic(pack_dir, index_path)
    assert ok is False
    assert "steps must be" in msg

def test_extra_steps_rejected(self, tmp_path):
    """Trace with 5 steps must be rejected (only 4 allowed)."""
    extra = [
        {"step": 1, "name": "init_params",      "hash": _VALID_HASH_A},
        {"step": 2, "name": "generate_dataset", "hash": _VALID_HASH_B},
        {"step": 3, "name": "compute_metrics",  "hash": _VALID_HASH_C},
        {"step": 4, "name": "threshold_check",  "hash": _VALID_HASH_D},
        {"step": 5, "name": "extra_step",       "hash": _VALID_HASH_E},
    ]
    pack_dir, index_path = _make_pack(
        tmp_path, execution_trace=extra, trace_root_hash=_VALID_HASH_E)
    ok, msg, errors = _verify_semantic(pack_dir, index_path)
    assert ok is False
    assert "must have exactly 4 steps" in msg

def test_fewer_steps_rejected(self, tmp_path):
    """Trace with 3 steps must be rejected (exactly 4 required)."""
    fewer = [
        {"step": 1, "name": "init_params",      "hash": _VALID_HASH_A},
        {"step": 2, "name": "generate_dataset", "hash": _VALID_HASH_B},
        {"step": 3, "name": "compute_metrics",  "hash": _VALID_HASH_C},
    ]
    pack_dir, index_path = _make_pack(
        tmp_path, execution_trace=fewer, trace_root_hash=_VALID_HASH_C)
    ok, msg, errors = _verify_semantic(pack_dir, index_path)
    assert ok is False
    assert "must have exactly 4 steps" in msg
```

PITFALL: The `_VALID_HASH_E` constant is needed for the extra-steps test (step 5 needs a hash, and trace_root_hash must match last step). Already-defined `_VALID_HASH_D` is used as the valid root for 4-step traces.
  </action>
  <verify>
    <automated>python -m pytest tests/steward/test_cert03_step_chain_verify.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/steward/test_cert03_step_chain_verify.py contains `def test_wrong_step_order_rejected`
    - tests/steward/test_cert03_step_chain_verify.py contains `def test_duplicate_step_numbers_rejected`
    - tests/steward/test_cert03_step_chain_verify.py contains `def test_extra_steps_rejected`
    - tests/steward/test_cert03_step_chain_verify.py contains `def test_fewer_steps_rejected`
    - tests/steward/test_cert03_step_chain_verify.py contains `_VALID_HASH_E`
    - `python -m pytest tests/steward/test_cert03_step_chain_verify.py -x -q` exits 0 with 10 tests passed (6 existing + 4 new)
    - `python -m pytest tests/ -q 2>&1 | tail -3` shows no regressions
  </acceptance_criteria>
  <done>4 new ordering invariant tests pass, proving _verify_semantic rejects misordered (1,3,2,4), duplicate (1,2,2,4), extra (5 steps), and fewer (3 steps) traces</done>
</task>

</tasks>

<verification>
```bash
# All 10 cert03 tests pass (6 existing + 4 new)
python -m pytest tests/steward/test_cert03_step_chain_verify.py -v --tb=short

# Steward audit still passes
python scripts/steward_audit.py

# Full suite no regressions
python -m pytest tests/ -q 2>&1 | tail -5
```
</verification>

<success_criteria>
- _verify_semantic in scripts/mg.py validates step count == 4 and step numbers == [1,2,3,4]
- 4 new tests prove misordered, duplicate, extra, and fewer step traces are rejected
- All existing tests pass (zero regressions)
- steward_audit.py passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-02-SUMMARY.md`
</output>
