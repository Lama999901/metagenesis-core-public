---
phase: 05-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/steward/test_step_chain_all_claims.py
autonomous: true
requirements:
  - CHAIN-01

must_haves:
  truths:
    - "All 14 claims have 6-test structural step chain coverage (trace_present, trace_four_steps, trace_deterministic, trace_changes_with_input, genesis_hash_derivation, inter_step_hash_linkage)"
    - "7 new claim test classes exist: ML_BENCH-01, ML_BENCH-02, ML_BENCH-03, PHARMA-01, FINRISK-01, DT-SENSOR-01, DT-CALIB-LOOP-01"
    - "7 existing claim classes are backfilled with genesis_hash and inter_step_linkage tests"
    - "All 391 existing tests continue to pass"
  artifacts:
    - path: "tests/steward/test_step_chain_all_claims.py"
      provides: "14 claim test classes x 6 tests each = 84 step chain structural tests"
      contains: "TestStepChainMLBENCH01"
  key_links:
    - from: "tests/steward/test_step_chain_all_claims.py"
      to: "backend/progress/*.py"
      via: "direct import of run_certificate/run_calibration functions"
      pattern: "from backend\\.progress\\."
---

<objective>
Extend step chain structural tests from 7 claims (28 tests) to 14 claims (84 tests), adding 7 new claim test classes and backfilling 2 new test methods (genesis_hash_derivation, inter_step_hash_linkage) to all 14 classes.

Purpose: Every claim must have verified step chain structure before subsequent phases can add ordering invariants and adversarial proofs. Without this foundation, CERT-11 cannot distinguish attack detection from structural failures.

Output: `tests/steward/test_step_chain_all_claims.py` with 14 claim classes x 6 tests = 84 tests
</objective>

<execution_context>
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/999ye/Downloads/metagenesis-core-public/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-foundation/05-CONTEXT.md
@.planning/phases/05-foundation/05-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs from the existing test file -->

From tests/steward/test_step_chain_all_claims.py (existing helpers):
```python
def _assert_trace_valid(result: dict, claim_id: str) -> None:
    """Assert execution_trace and trace_root_hash are present and valid."""

def _get_trace_root(result: dict) -> str:
    """Extract trace_root_hash from result dict."""
```

From tests/steward/test_step_chain_all_claims.py (existing class pattern):
```python
class TestStepChainMTR1:
    def _run_mtr1(self):
        from backend.progress.mtr1_calibration import run_calibration
        return run_calibration(seed=42, E_true=70e9, n_points=50, max_strain=0.002)

    def test_mtr1_trace_present(self): ...
    def test_mtr1_trace_four_steps(self): ...
    def test_mtr1_trace_deterministic(self): ...
    def test_mtr1_trace_changes_with_seed(self): ...
```

New claim function signatures (from RESEARCH.md):
```python
# ML_BENCH-01: from backend.progress.mlbench1_accuracy_certificate import run_certificate; run_certificate(seed=42)
# ML_BENCH-02: from backend.progress.mlbench2_regression_certificate import run_certificate; run_certificate(seed=42)
# ML_BENCH-03: from backend.progress.mlbench3_timeseries_certificate import run_certificate; run_certificate(seed=42)
# PHARMA-01: from backend.progress.pharma1_admet_certificate import run_certificate; run_certificate(seed=42)
# FINRISK-01: from backend.progress.finrisk1_var_certificate import run_certificate; run_certificate(seed=42)
# DT-SENSOR-01: from backend.progress.dtsensor1_iot_certificate import run_certificate; run_certificate(seed=42)
# DT-CALIB-LOOP-01: from backend.progress.dtcalib1_convergence_certificate import run_certificate; run_certificate(seed=42)
```

_hash_step importability (CRITICAL):
- 7 OLDER claims (MTR-1/2/3, SYSID-01, DATA-PIPE-01, DRIFT-01, DT-FEM-01): _hash_step is NESTED inside run function, NOT importable
- 7 NEWER claims (ML_BENCH-01/02/03, PHARMA-01, FINRISK-01, DT-SENSOR-01, DT-CALIB-LOOP-01): _hash_step is module-level, importable via `from backend.progress.<module> import _hash_step`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 7 new claim test classes with full 6-test pattern</name>
  <files>tests/steward/test_step_chain_all_claims.py</files>
  <read_first>
    - tests/steward/test_step_chain_all_claims.py (current file with 7 existing classes)
    - backend/progress/mlbench1_accuracy_certificate.py (verify run_certificate signature and _hash_step is module-level)
    - backend/progress/mlbench2_regression_certificate.py (verify run_certificate signature)
    - backend/progress/pharma1_admet_certificate.py (verify run_certificate signature)
    - backend/progress/finrisk1_var_certificate.py (verify run_certificate signature)
    - backend/progress/dtsensor1_iot_certificate.py (verify run_certificate signature)
    - backend/progress/dtcalib1_convergence_certificate.py (verify run_certificate signature)
    - backend/progress/mlbench3_timeseries_certificate.py (verify run_certificate signature)
  </read_first>
  <behavior>
    - Test: TestStepChainMLBENCH01.test_mlbench1_trace_present -- run_certificate(seed=42) returns result with valid execution_trace and trace_root_hash
    - Test: TestStepChainMLBENCH01.test_mlbench1_trace_four_steps -- execution_trace has exactly 4 steps
    - Test: TestStepChainMLBENCH01.test_mlbench1_trace_deterministic -- two runs with same seed produce identical trace_root_hash
    - Test: TestStepChainMLBENCH01.test_mlbench1_trace_changes_with_input -- seed=42 vs seed=99 produce different trace_root_hash
    - Test: TestStepChainMLBENCH01.test_mlbench1_genesis_hash -- step 1 hash recomputable via imported _hash_step("init_params", result["inputs"], "genesis")
    - Test: TestStepChainMLBENCH01.test_mlbench1_inter_step_linkage -- all 4 step hashes are distinct (non-trivial chain) and deterministic across runs
    - Same 6-test pattern for ML_BENCH-02, ML_BENCH-03, PHARMA-01, FINRISK-01, DT-SENSOR-01, DT-CALIB-LOOP-01
  </behavior>
  <action>
Add 7 new test classes to the END of tests/steward/test_step_chain_all_claims.py, after the existing TestStepChainDTFEM01 class. Each class follows this exact pattern:

1. Add a `_recompute_hash_step` module-level helper at the top (after existing helpers):
```python
def _recompute_hash_step(step_name, step_data, prev_hash):
    """Independent reimplementation of _hash_step for genesis verification."""
    import hashlib, json as _j
    content = _j.dumps({"step": step_name, "data": step_data, "prev_hash": prev_hash},
                       sort_keys=True, separators=(",", ":"))
    return hashlib.sha256(content.encode("utf-8")).hexdigest()
```

2. For each of the 7 new claims, add a class with 6 tests. Example for ML_BENCH-01:
```python
class TestStepChainMLBENCH01:
    def _run(self):
        from backend.progress.mlbench1_accuracy_certificate import run_certificate
        return run_certificate(seed=42)

    def test_mlbench1_trace_present(self):
        _assert_trace_valid(self._run(), "ML_BENCH-01")

    def test_mlbench1_trace_four_steps(self):
        assert len(self._run()["execution_trace"]) == 4

    def test_mlbench1_trace_deterministic(self):
        assert self._run()["trace_root_hash"] == self._run()["trace_root_hash"]

    def test_mlbench1_trace_changes_with_input(self):
        from backend.progress.mlbench1_accuracy_certificate import run_certificate
        assert run_certificate(seed=42)["trace_root_hash"] != run_certificate(seed=99)["trace_root_hash"]

    def test_mlbench1_genesis_hash(self):
        """Step 1 hash derives from _hash_step('init_params', inputs, 'genesis')."""
        from backend.progress.mlbench1_accuracy_certificate import _hash_step
        result = self._run()
        trace = result["execution_trace"]
        recomputed = _hash_step("init_params", result["inputs"], "genesis")
        assert trace[0]["hash"] == recomputed

    def test_mlbench1_inter_step_linkage(self):
        """All 4 step hashes are distinct (non-trivial chain)."""
        result = self._run()
        hashes = [s["hash"] for s in result["execution_trace"]]
        assert len(set(hashes)) == 4, "Step hashes must all be distinct"
```

3. Replicate for the remaining 6 claims using these imports and claim IDs:
   - ML_BENCH-02: `from backend.progress.mlbench2_regression_certificate import run_certificate, _hash_step` / claim_id="ML_BENCH-02" / test prefix `test_mlbench2_`
   - ML_BENCH-03: `from backend.progress.mlbench3_timeseries_certificate import run_certificate, _hash_step` / claim_id="ML_BENCH-03" / test prefix `test_mlbench3_`
   - PHARMA-01: `from backend.progress.pharma1_admet_certificate import run_certificate, _hash_step` / claim_id="PHARMA-01" / test prefix `test_pharma1_`
   - FINRISK-01: `from backend.progress.finrisk1_var_certificate import run_certificate, _hash_step` / claim_id="FINRISK-01" / test prefix `test_finrisk1_`
   - DT-SENSOR-01: `from backend.progress.dtsensor1_iot_certificate import run_certificate, _hash_step` / claim_id="DT-SENSOR-01" / test prefix `test_dtsensor1_`
   - DT-CALIB-LOOP-01: `from backend.progress.dtcalib1_convergence_certificate import run_certificate, _hash_step` / claim_id="DT-CALIB-LOOP-01" / test prefix `test_dtcalib1_`

All new claims use `run_certificate(seed=42)` as default and `run_certificate(seed=99)` for changes_with_input.

PITFALL: All 7 new claims have module-level `_hash_step`, so direct import is safe. Do NOT attempt `from backend.progress.mtr1_calibration import _hash_step` -- that will fail because it is nested.
  </action>
  <verify>
    <automated>python -m pytest tests/steward/test_step_chain_all_claims.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainMLBENCH01`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainMLBENCH02`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainMLBENCH03`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainPHARMA01`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainFINRISK01`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainDTSENSOR01`
    - tests/steward/test_step_chain_all_claims.py contains `class TestStepChainDTCALIBLOOP01`
    - tests/steward/test_step_chain_all_claims.py contains `def _recompute_hash_step`
    - Each new class has exactly 6 test methods (trace_present, trace_four_steps, trace_deterministic, trace_changes_with_input, genesis_hash, inter_step_linkage)
    - `python -m pytest tests/steward/test_step_chain_all_claims.py -x -q` exits 0 with 70+ tests passed
  </acceptance_criteria>
  <done>7 new claim test classes added with 42 new tests (7 claims x 6 tests), all passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill genesis_hash and inter_step_linkage tests for 7 existing claims</name>
  <files>tests/steward/test_step_chain_all_claims.py</files>
  <read_first>
    - tests/steward/test_step_chain_all_claims.py (after Task 1 modifications)
    - backend/progress/mtr1_calibration.py (verify _hash_step is nested, find init_params data structure)
    - backend/progress/mtr2_thermal_conductivity.py (verify _hash_step is nested)
    - backend/progress/drift_monitor.py (verify no seed parameter, uses current_value)
    - backend/progress/datapipe1_quality_certificate.py (verify _hash_step is nested, check dataset_relpath usage)
  </read_first>
  <behavior>
    - Test: TestStepChainMTR1.test_mtr1_genesis_hash -- step 1 name is "init_params", hash is deterministic, hash is 64 hex chars
    - Test: TestStepChainMTR1.test_mtr1_inter_step_linkage -- all 4 step hashes distinct
    - Same pattern for MTR-2, MTR-3, SYSID-01, DATA-PIPE-01, DRIFT-01, DT-FEM-01
  </behavior>
  <action>
Add 2 test methods to each of the 7 EXISTING test classes in tests/steward/test_step_chain_all_claims.py:

For the 7 OLDER claims (where `_hash_step` is nested and NOT importable), use STRUCTURAL verification instead of recomputation:

1. `test_<prefix>_genesis_hash` -- Verify structurally:
```python
def test_mtr1_genesis_hash(self):
    """Step 1 hash derives from genesis (structural verification)."""
    result = self._run_mtr1()
    trace = result["execution_trace"]
    # Step 1 must be named "init_params"
    assert trace[0]["name"] == "init_params"
    # Genesis hash must be valid 64-char hex
    assert len(trace[0]["hash"]) == 64
    # Genesis hash must be deterministic
    result2 = self._run_mtr1()
    assert trace[0]["hash"] == result2["execution_trace"][0]["hash"]
    # Genesis hash must differ with different inputs (proves it derives from params, not constant)
```

For the "differs with inputs" part of genesis, call the run function with different params:
- MTR-1: `run_calibration(seed=99, E_true=70e9, n_points=50, max_strain=0.002)` -- compare step 0 hash
- MTR-2: `run_calibration(seed=99, k_true=5.0, n_points=50)` -- compare step 0 hash
- MTR-3: `run_calibration(seed=99, k_true=5.0, r_contact_true=0.1, n_points=50)` -- compare step 0 hash
- SYSID-01: `run_calibration(seed=99, a_true=0.9, b_true=0.5, n_steps=50)` -- compare step 0 hash
- DATA-PIPE-01: Cannot vary seed easily (uses CSV fixture), so verify structural properties only (name=="init_params", 64 hex, deterministic)
- DRIFT-01: `run_drift_monitor(anchor_value=70e9, current_value=71e9, drift_threshold_pct=5.0)` -- compare step 0 hash
- DT-FEM-01: `run_certificate(seed=99, reference_value=1.0, rel_err_threshold=0.02)` -- compare step 0 hash

2. `test_<prefix>_inter_step_linkage` -- Verify all 4 step hashes are distinct:
```python
def test_mtr1_inter_step_linkage(self):
    """All 4 step hashes are distinct (non-trivial chain)."""
    result = self._run_mtr1()
    hashes = [s["hash"] for s in result["execution_trace"]]
    assert len(set(hashes)) == 4, "Step hashes must all be distinct"
```

CRITICAL PITFALL: Do NOT attempt `from backend.progress.mtr1_calibration import _hash_step` -- it will raise ImportError because `_hash_step` is a nested function. Use structural verification only for these 7 claims.

PITFALL: DRIFT-01 has NO `seed` parameter. Use `current_value` variation instead.
PITFALL: DATA-PIPE-01 requires fixture file `tests/fixtures/data01/al6061_stress_strain_sample.csv`.
  </action>
  <verify>
    <automated>python -m pytest tests/steward/test_step_chain_all_claims.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/steward/test_step_chain_all_claims.py contains `test_mtr1_genesis_hash`
    - tests/steward/test_step_chain_all_claims.py contains `test_mtr1_inter_step_linkage`
    - tests/steward/test_step_chain_all_claims.py contains `test_mtr2_genesis_hash`
    - tests/steward/test_step_chain_all_claims.py contains `test_drift_genesis_hash`
    - tests/steward/test_step_chain_all_claims.py contains `test_dtfem_genesis_hash`
    - tests/steward/test_step_chain_all_claims.py contains `test_datapipe_genesis_hash`
    - `python -m pytest tests/steward/test_step_chain_all_claims.py -x -q` exits 0 with 84 tests passed (14 classes x 6 tests)
    - `python -m pytest tests/ -q 2>&1 | tail -3` shows no regressions (total >= 391 + 56 new)
  </acceptance_criteria>
  <done>All 14 claims have uniform 6-test coverage (84 total tests in file), all passing, zero regressions</done>
</task>

</tasks>

<verification>
```bash
# All 84 step chain tests pass
python -m pytest tests/steward/test_step_chain_all_claims.py -v --tb=short 2>&1 | tail -20

# No regressions in full suite
python -m pytest tests/ -q 2>&1 | tail -5

# Verify 14 test classes exist
grep -c "class TestStepChain" tests/steward/test_step_chain_all_claims.py
# Expected: 14
```
</verification>

<success_criteria>
- 14 test classes exist in test_step_chain_all_claims.py (one per claim)
- Each class has 6 test methods
- All 84 tests pass
- Full test suite passes with zero regressions
- Genesis hash verification uses structural checks for older claims, recomputation for newer claims
</success_criteria>

<output>
After completion, create `.planning/phases/05-foundation/05-01-SUMMARY.md`
</output>
